# A — 공통 준비·작은 실제 데이터 평가

2026-09-23. 진행 중 기록이며 마지막 검증 후 결과·커밋을 보완한다.

## 기준과 버전

- 작업 브랜치 `claude/intent-contract-0922`(기존 세션 브랜치 유지).
- 시작 HEAD `8771c06`; 관련 미커밋 계획 7개 파일만 `a82ddf4`에 고정. 무관한 변경 없음.
- C0 공통 기준 커밋: 이 문서와 `docs/contracts/`를 처음 추가한 커밋(`git log --diff-filter=A --format=%H -- docs/contracts/C0-v1.md`로 확인).
- 계약 `C0-v1`, 의도 기존 v1, 가치 `defense-we-pa-v1`, 모델 `observer-zone-v1` / 번들 `minimal-pitch-service-v1`, standalone runtime v1.
- 모델 경로·전체 해시·실행 명령: `docs/contracts/model-v1.json`. 실제 실행 입출력 `docs/contracts/examples/model-v1.json`; 합성 의도 9사례 `docs/contracts/examples/c0-v1.json`.
- 데이터·평가 사양 `docs/contracts/S0-S1-v1.md`, 실험 `EXP-A-S0S1-001`. S0/S1 실제 결과는 아직 계산 전.

## C0 실제 검증

고정 번들의 전처리/WE/5개 seed 체크포인트를 복원하고 실제 기존 재생 데이터 첫 공 `776703:1:1`에서 추천 ready를 확인했다. 새 Recommender 인스턴스로 복원/캐시 재사용 결과 동일, actual 필드 변조 불변, 확률→%p 변환 일치. 기존 모델/원본은 덮어쓰지 않았고 새 캐시는 SSD `runs/A-C0-v1`에 격리했다.

합성 의도는 기존 builder/validator를 사용한다. 실제 영상·실측 라벨·제구 성능 표본이 아니며, 전체 좌표 사슬도 가상 보정이다. 구조 검증 통과와 수신 계층의 투구 연결/시점 검사를 구분한다.

## B/C/D 시작 범위

- **B 추천 어댑터 개발 시작 가능:** C0/고정 모델/입출력 예시 준비. 후보 확률과 action 대응을 공개하는 B1, 기존 결과 재현·제구 모형 교체 경계가 범위다. 새 학습은 작은 자료 manifest/기준선 실행 후 별도 알림.
- **C 사건 기여도 개발 시작 가능:** 동일 WE 관점·단위·타석 범위와 개발 의도 샘플로 C1/C2 시작. B 새 학습이나 실제 CV를 기다리지 않는다. **C는 분석 결과 형식을 먼저 확정해 D에 전달해야 한다.** 실제 제구/기여도 품질은 실제 CV·라벨 인수에 의존한다. 교체는 당시 가능한 명단·범위 자료가 추가로 필요하다.
- **D 기본 관전 화면 개발 시작 가능:** 기존 API·사전 추천 불변 저장·동일 투구 실제 비교로 D1 시작. **C 결과 형식은 아직 미수신**이므로 사건 카드 D2 연결은 그 형식에 의존한다. 실제 CV와 C 계산 완료를 가정하지 않는다.
- B/C/D 별도 작업은 사용자가 만든다. A가 생성하거나 대신 구현하지 않았다.

## 역할·사용량

- 주 에이전트는 Astra 역할로 계약·가치·선정/누수·핵심 검토·시작 판정을 담당. 이 도구 출력은 주 에이전트의 정확한 런타임 모델 ID를 노출하지 않아 Astra 모델 실행 여부 자체는 독립 확인하지 못함.
- 모델 지정 지원 확인: collaboration.spawn_agent의 model/reasoning_effort 사용. 실제 지정된 서브에이전트는 `gpt-6-sol`, `medium` 2개: `c0_fixtures`(합성 샘플·검사), `s1_path`(기존 자료 점검 및 작은 평가 구현·실행). 전체 대화 상속 없이 필요한 파일·제약만 전달. 재귀 위임 없음.
- 세션/에이전트별 토큰·비용의 확인 가능한 도구 값 없음: **미측정**. 실행 시간·최대 RSS는 실행 결과에 실측으로 기록한다.

## 실행 상태와 다음 한 가지

시작 시 프로젝트 학습·서버 프로세스 없음. 대규모 학습 시작 안 함. C0 로그 `runs/A-C0-v1/verification.log`; S0/S1 출력은 별도 `runs/A-S0S1-001`. 다음 한 가지는 고정한 경기 manifest를 커밋한 뒤 S0/S1 기준선을 실행하는 것이다.
