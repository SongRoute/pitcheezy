# Independent review — CHOICE-DIAG-002

Reviewed B's finalized `b197e1c` diagnosis implementation and its preregistered `885bb768` config/contract without editing B's files or running model inference. This review is separate from the authoring agent. The full result is the immutable SSD `CHOICE-DIAG-002/results.json`; `results/CHOICE-DIAG-002/summary.json` in B's branch is a compact copy. The earlier `results.pre-audit.json` remains preserved. The source cohort is the previously selected six July 2025 DEV games, already exposed for development; this is descriptive diagnosis, not held-out policy evaluation.

## Findings and checks

- Denominators trace from 1,690 full-game pitches to 160 target-pitcher PAs/597 pitches, then 151 eligible complete PAs/563 pitches. Nine PAs/34 pitches are excluded: five unsupported PAs, three state changes within PA, one missing terminal. A separate source check found zero mixed-pitcher target PAs in this frozen cohort; B now labels strictly selected-pitcher rows and also reports associated other-pitcher rows. All 151 evaluation records were ready and every eligible observed type had a supported zone. The 100% figure is conditional on 563 eligible pitches; whole-target eligible-and-supported coverage is 563/597.
- The evaluator constructs each PA once from its first pre-pitch state, then indexes the computed 12-count Q tensor at each observed pitch's **preceding** count. The existing complete-PA eligibility rule rejects changes in inning, half, outs, bases, score, batter, and stand within a PA. April TRAIN supplies repertoire counts and median zone bounds; frozen batter profiles predate July. The replay adapter allowlists request, zone bounds, and repertoire counts, so actual type/location/outcome are comparison fields, not inference inputs. No future selected-game result is read into the request. The frozen Engine verifies bundle artifacts against its manifest; B additionally checks metadata and the fixed TRAIN/DEV parquet hashes.
- `plan.q_values[balls,strikes,0]` is defensive-team PA WE under the frozen model with the model's optimized future count continuation. Ranking traverses every supported pitch-type×zone action at unrounded Q and resolves exact ties by original action index. The actual observed type uses its **best supported zone**; it is neither the actual location's value nor a measured intended target. I independently recomputed action rank, unique-type rank, and `100 × (best Q − best actual-type Q)` from all 151 stored PA checkpoints: 906 ensemble/member evaluations, 3,378 pitch-by-variant records, zero mismatches. Supported action counts ranged from 40 to 48. The first action matched the actual type on 147/563 pitches; 324/563 actual-type gaps were at most 0.05 percentage points. This validates arithmetic, not the model's real-world value estimates.
- Type concentration uses the same evaluable pitches for observed and advised type distributions, with all/pitcher/count/pitcher×count strata. It counts the top *action's type* once per pitch, without interpreting the top three zone actions as three types. Type×zone support rates use eligible PAs for each pitcher as denominator. ESS/mass are reported only where the public adapter retained that action; the seven never-retained cells have null ESS/mass rather than fabricated values. Kernel ESS is not off-policy evaluation ESS.
- Seed42–46 are existing independently trained frozen members with the same delivery pools, frequency component, blend weight, action support, and member-specific saved calibration. `evaluate_pre_pitch` calls `_compute` directly and does not read/write the Observer recommendation cache, so changing `engine.models` for this serial research loop cannot retrieve an ensemble result from a disk cache. The adapter's bundle-level `model_identity` and `model_sha256` stay the same for the one-member calls; B now separately reports each seed/checkpoint SHA and its calibration. This member spread is a sensitivity check, not a bootstrap CI or full-pipeline retraining uncertainty.
- B's final aggregation validates each checkpoint's config and PA key, ordered pitch keys/count/type, April repertoire and bounds, six expected variants, common action support, bundle/adapter identity, and saved Q-to-rank arithmetic before reuse. It refuses to overwrite an existing final `results.json`. Checkpoints were produced before per-file producer-code hashes were recorded; the report states that provenance limit and does not retroactively claim a historical script hash. The final report contains exploratory paired game-cluster bootstrap intervals from only six exposed DEV games, and separates prediction-quality references, internal Q values, and null OPE/causal policy value.

I ran `PYTHONPATH=.:apps/observer/backend /Users/song/Projects/pitcheezy/.venv-observer-standalone/bin/python -m pytest tests/test_b_choice_metrics.py -q` in B's worktree: five passed. I also verified that every stored pitch key belongs to the fixed DEV parquet and independently checked the full-action arithmetic as above. No heavy inference was run for this review.

## Interpretation boundary

The observed mixture differs from the model's top-action mixture, and five members are unanimously top-action aligned on about one third of eligible pitches. These observations motivate limited offline screens. They do not identify how much of the difference comes from optimization, missing intent, command, opponent response, or model error. Actual-type agreement is diagnostic only. No model adoption or substitution/policy advantage follows from this audit.

## Subsequent candidate screen review — CHOICE-CANDIDATES-003

A's `23b9de4` preregistration fixed exactly two rules after viewing the first diagnosis and before candidate outputs. I reviewed B's final `7f90b5e` code/result. The runner pins the SSD diagnosis result SHA to the committed diagnosis summary, verifies every PA checkpoint SHA, checks all six variant labels and identical action identities/order, and refuses an existing output directory. The code uses only cached Q and April repertoire counts; no new model inference or training was needed. The first candidate output remains preserved separately as `CHOICE-CANDIDATES-003-preaudit`.

The repertoire screen chooses the most frequent April type among actions no more than **0.05 percentage points** below the ensemble's maximum unrounded Q, resolving remaining ties by Q then original index. The minimax screen minimizes the maximum of each member's *within-member* regret relative to that member's own best Q, resolving ties by ensemble Q then index. This subtracts away differences in members' absolute WE levels. Both choices require the exact same supported action array in all six judges; no support intersection or new target is inferred.

In each of five leave-one-member-out folds, the held-out member is removed before the rule selects an action. The reference is the argmax of the remaining four members' arithmetic mean Q, and the held-out member's Q scores candidate minus reference. This arithmetic mean Q is a sensitivity control, not the frozen ensemble Q or a reconstructed ensemble model. A synthetic leakage test makes the excluded member strongly favor the opposite action; it passes. I independently recomputed the full selected action, ensemble contrast, and all five held-out choices/scores for all **1,126 candidate-pitch rows**, with zero mismatches against SSD `CHOICE-CANDIDATES-003/results.json`. Four focused candidate tests pass.

| Candidate | Changed actions/types | Mean ensemble current-action Δ (%p) | Mean worst-member regret Δ (%p) | Mean held-out member Δ (%p) | Preregistered decision |
|---|---:|---:|---:|---:|---|
| Near-value repertoire | 221 / 221 | −0.009174 | +0.015004 | −0.008522 | Reject |
| Minimax member regret | 64 / 30 | −0.000771 | −0.001003 | −0.000429 | Reject |

The paired six-game bootstrap intervals are exploratory and both ensemble mean contrasts are negative, so the fixed rule rejects both for service adoption. The frozen baseline already chooses the ensemble's maximum-Q action: a changed action cannot raise that **same model's current-action Q** by construction. These screens quantify the internal Q cost of concentration or member-regret changes; they do not test whether a different policy improves actual outcomes. Each stored Q assumes that judge's own optimized future count continuation after the current action. It is not a repeated-action/full-PA rollout value. Off-policy and causal policy values remain null. Actual-type match changes are descriptive only and do not override rejection.
