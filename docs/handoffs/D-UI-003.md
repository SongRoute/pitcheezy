# D-UI-003 — Observer 정보 구조 개편

2026-09-24. 작업 브랜치 `codex/d-ui-v3`, 시작 커밋 `122b421`. 실제 실행 모델은 `gpt-6-sol/medium`. 토큰·비용 수치는 제공되지 않아 기록하지 않는다. 동결 모델·CV 경로·이닝 API 계약은 변경하지 않았다.

## 결과

관전 화면의 첫 읽기 순서를 **다음 추천 → 같은 공의 실제 비교 → 사건 설명**으로 옮겼다. 큰 모델 제안 카드에 투수·타자, 저장 시점, 선택한 공 번호·투구 전 카운트와 다음 실제 공 확인 버튼을 표시한다. 390px 화면에서 버튼 전체가 첫 844px 안에 들어온다. 다음 공과 공개된 과거 공을 전환하는 버튼은 카드 안에 상시 보인다. 공개된 실제 공과 추천은 같은 `pitch_id`에 저장된 추천으로 비교하며, 실제 기록은 관측, 추천과 승률 변화는 모델 추정, 투구 의도·선수별 기여는 미측정으로 표시한다. 마지막 공의 사건은 이전 공을 타임라인에서 고른 상태와 혼동하지 않도록 공 번호를 붙였다. 작은 내부 WE 차이가 실전 우위를 증명하지 않는다고 안내한다.

구역 그림·투수 구속, 후보 근거·수치, 사건 분해는 펼치기에서 접근할 수 있다. 공개된 공 타임라인, 수동 목표 메모, 모델 안내, 영상 검토실, 교체 직전 이닝 화면은 계속 접근 가능하다. 이닝 화면은 독립 과거 기록의 현재 투수 유지 조건부 전망이며 실제 교체 우위로 표시하지 않는다.

## 별도 확인 경로와 검증

- 새 실행 공간: `/Volumes/T7 Shield/pitcheezy/pitchmdp/runs/D-UI-003/`.
- 기존 관전 서버 `127.0.0.1:8766`은 건드리지 않았다. dataset/manifest 및 추천 캐시는 별도 경로에 복사했다. **변경 중인 SQLite는 Python `sqlite3.Connection.backup()`으로 복제**하고 복제 DB의 `pragma integrity_check=ok`를 확인했다. [준비 일치 기록](/Volumes/T7%20Shield/pitcheezy/pitchmdp/runs/D-UI-003/preparation-equality.json)에서 원본과 복제의 7개 테이블 행 수·내용 해시가 모두 같다(세션 22, 추천 56, 사건 12, 메모 8 포함). 새 서버는 별도 DB/캐시와 worktree 빌드 자산을 쓰며 `127.0.0.1:8767`에서 동작한다.
- URL: 변경 전 `http://127.0.0.1:8766/`, 변경 후 `http://127.0.0.1:8767/`, 이닝 `http://127.0.0.1:8767/inning-decisions`.
- 맥북에서 보기: `ssh -N -o ExitOnForwardFailure=yes -L 18767:127.0.0.1:8767 macmini` 후 `http://127.0.0.1:18767/`.
- 화면: [변경 전 데스크톱](/Volumes/T7%20Shield/pitcheezy/pitchmdp/runs/D-UI-003/browser/before-desktop.png), [변경 후 데스크톱](/Volumes/T7%20Shield/pitcheezy/pitchmdp/runs/D-UI-003/browser/after-desktop.png), [변경 전 390px](/Volumes/T7%20Shield/pitcheezy/pitchmdp/runs/D-UI-003/browser/before-mobile-390.png), [변경 후 390px](/Volumes/T7%20Shield/pitcheezy/pitchmdp/runs/D-UI-003/browser/after-mobile-390.png), [다음 공 390px](/Volumes/T7%20Shield/pitcheezy/pitchmdp/runs/D-UI-003/browser/after-mobile-next-390.png).
- 재현: `npm run build --prefix apps/observer/web`; `PLAYWRIGHT_BROWSERS_PATH='/Volumes/T7 Shield/pitcheezy/pitchmdp/runs/observer-mvp-v1/browser-cache' node apps/observer/web/tests/ui-v3-smoke.mjs`. 결과 JSON은 `D-UI-003/browser/ui-v3-report.json`.

빌드 및 브라우저 검사가 통과했다. 동일 저장 세션의 데스크톱·390px 실제 화면, 타임라인의 과거 공 선택, 미완료 타석의 다음/과거 시점 전환, 새로고침 복원, 오류/재시도, 이닝 경로를 확인했다. 별도 서버를 재시작한 뒤 health 200과 동일 저장 세션 revision 4의 HTTP 복원도 확인했다. 현재 preview PID는 `6028`이며 접속 상태를 유지한다. 브라우저 페이지 오류 0건, 390px 문서 너비 390px, 첫 화면 버튼 하단 828px/화면 844px, 측정된 초기 화면 로드 552ms(데스크톱)·569ms(모바일)였다. 이는 로컬 단일 관측이며 지연 분포나 사용 환경 성능을 입증하지 않는다. Browser skill을 읽고 해당 도구를 탐색했으나 세션에 필요한 `node_repl js`가 노출되지 않아 저장소의 Playwright Chromium으로 실제 브라우저 검사를 수행했다.

## 팬 이해 평가 준비

자동 브라우저 통과를 팬 이해로 세지 않는다. 팬에게 먼저 추천 화면에서 다음 공과 이미 던진 공을 구분하게 하고, 3구 비교에서 실제 구종과 저장 추천 구종을 각각 말하게 한다. 이어 `+1.20%p`가 실제 추천 효과/선수 책임인지 묻고, 교체 직전 화면이 실제 교체 효과를 제공하는지 답하게 한다. 판정 기준은 네 과제 모두 근거 위치를 가리켜 맞게 설명하고, 실제/추정/미측정을 섞지 않는 것이다. 정답률·소요 시간·혼동 지점을 별도로 기록해야 하며 아직 참가자 검사는 없다.

남은 의존성: C의 후보 근거 가용성 결과가 확정되면 이닝 화면에는 검증된 범위만 연결한다. 새 모델의 추천 품질 우위와 CV 의도는 이번 UI 검증으로 판정하지 않는다.
