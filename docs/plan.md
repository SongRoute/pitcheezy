# 계획

## 앵커 (고정)
- 최종 발표: 2026-11-24 (화)
- 자체 마감: 2026-10-31. 이 날짜는 상한이다. 빨리 끝나면 끝내고, 남는 시간은 다음 개선 축으로 넘어간다 (D28). 11월은 보고서·버퍼
- P2 컷라인: 2026-10-19 (월). 이후 새 실험 시작 안 함
- 2026 검증셋: 정규시즌 종료(9/27) 직후 `--freeze`로 1회 갱신 후 고정. freeze 를 기다리지 않는다: 그 전에는 현재 수집본(d20260911)으로 개발·판정을 계속하고, freeze 뒤 채택된 실험만 고정본으로 재실행해 재확인한다 (D28)
- 실행: 맥미니 로컬 + W&B (D10). Colab Pro는 예비
- 방식: 주 단위 롤링. 주차표는 상한이며 앞 주 일이 끝나면 다음 주 일을 바로 당겨온다 (D28). 매주 월요일 아래 "이번 주" 블록만 갱신. 끝까지 미리 짜지 않는다

## 하루 루틴 (D11)
1. **아침 (10분, 폰이나 맥북)** 새 세션. 첫 메시지: "밤사이 결과 확인". Claude가 results/·runs/_logs/를 읽고 experiments.md에 행을 채우고, 오늘 할 한 가지를 제안한다
2. **낮** 그 한 가지를 진행. 코드 변경은 테스트 통과 → 커밋
3. **저녁 (10분)** 다음 밤에 돌릴 실험을 config로 만들고 runs 창에서 걸어둔다. 결과 파일 경로를 plan.md "다음 아침 확인" 줄에 적는다
4. **월요일** 이번 주 블록 갱신

밤 실험이 없는 날은 아침 확인이 비므로, 저녁에 걸어둘 것이 없으면 "없음"이라고 적는다. 실험이 실패해도 행을 남긴다.

**다음 아침 확인** (2026-09-18): `docs/results-p0.md` (P0 결과표 초안 — 실험 8개, 짝지은 비교 표, 위약·민감도) → `docs/experiments.md` EXP-P0-001/002/003 행 → `docs/decisions.md` D16~D20 잠정 결정. 로그 `runs/_logs/EXP-P0-00*_s*.log`, 요약 `results/EXP-P0-00*.json`, W&B pitcheezy/{ID}/s{seed}. 실행 `runs/_logs/run_p0.sh` (전체 5분).

## Phase
- **P0** 베이스라인 e2e. 우리 모델 + B1(SmartPitch류) + π_b·랜덤 기준선을 같은 표에. 결과: 표 하나
- **P1** 개선 축 ablation. 순서(D24): 투수별 구조 ⓑ → 시퀀스 맥락(B2 재구현과 함께) → xRE24 + 결과 세분화. 축 하나씩, 짝지은 OPE(D22) 오르면 채택
- **P2** 남는 만큼. 4자 분해(D7), 포스트시즌 데모 슬라이스(D12), 폭투/포일, 시즌 창, 보수화

## 주차별 (초안, 2026-09-15)

| 주 | 내용 |
|---|---|
| W2 9/14–20 | 규칙·문서 전환(D5~D9), interfaces/ 코드화 + 계약 테스트 + fixtures, RE24·dRE24 표, 레귤러 선발 필터, 타자 군집 v0 |
| W3 9/21–27 | 전이 모델 ⓐ(EXP-P0-001) + B1(EXP-P0-002), VI, π_b 추정, IPS OPE, run_experiment.py |
| W4 9/28–10/4 | 2026 freeze 수집. P0 결과표(우리 / B1 / π_b / 랜덤). 태그 p0-baseline |
| W5–W6 10/5–18 | (D24) 투수별 구조 ⓑ 공유 모델+투수 임베딩 → B2 재구현(Transformer), 시퀀스 맥락 축, 2스트라이크 OPE 비교 |
| W7 10/19–25 | xRE24 + 결과 세분화 또는 ⓑⓒ 중 하나. 채택 후보 5시드 |
| W8 10/26–31 | 채택 판정, 표·그림, decisions 정리 |
| 11월 | 보고서. 여유 있으면 4자 분해 |

리스크: W5–W6에 B2와 시퀀스 축이 같이 들어 있어 하나가 밀리면 W7 축이 0개가 된다. 그래도 P0 표 하나만 나오면 목표의 뼈대는 선다.

## 이번 주 (W2, 9/14–20)

**필수**
1. ~~규칙·문서 전환 PR~~ (9/17 머지, PR #5)
2. ~~`src/pitcheezy/interfaces/` 코드화: states, pitch_types, outcomes, grid, tensor, validate, value.lookup. `tests/interfaces/` 계약 테스트로 교체~~ (9/17 완료, D13)
3. ~~`tests/fixtures/` 소형 parquet (투수 2명 × ~100구), 출처 버전 기록~~ (9/17 완료)
4. ~~RE24 테이블(2023–25) + dRE24 표 산출, 버전 부여~~ (9/17 완료, D14, `data/re24/re24-2023_2025-d20260911-s2325-82b008b`)
5. ~~레귤러 선발 필터(시즌 100이닝) → 투수 목록·투구 수 문서화~~ (9/17 완료, D15, 시즌 ~127명·풀 219명)

**빨리 끝나면**
- 타자 군집 v0 (좌우 층화, K ≤ 8)
- 타석 중 주자·아웃 변화 비율 실측 (design.md §1-e)

**빨리 끝나면**
- ~~타석 중 주자·아웃 변화 비율 실측~~ (9/17, ASM-1 실측 기록)

**W3 항목을 9/17 밤에 당겨서 완료** (PR #10): 전이 모델 ⓐ + 계층 평활, VI, π_b(교차 적합), 1스텝·궤적 SNIPS OPE, run_experiment.py, EXP-P0-001/002/003 시드 3개. 결과 docs/results-p0.md. 남은 W3: 없음. W4 로: freeze(9/27 이후) → 재실행 → p0-baseline 태그
