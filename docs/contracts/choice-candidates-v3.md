# 진단 후 두 후보 사전 고정 — 2026-09-24

진단 사양 `885bb76` 이후 첫 결과를 A가 검토했다. 같은 563구에서 실제 구종의 최고 지원 행동과 전체 최고 행동의 가치 차이는 중앙값 0.03922 %p, 기존 5개 학습 구성원의 최고 행동 완전 일치는 33.57%다. Webb 실제 싱커 109/269구 대비 추천 싱커 64/269, 스위퍼 실제 59/269 대비 추천 122/269로 배합도 다르다. 이 관측은 정책 오류나 우월성의 증명이 아니다.

이 결과를 보고 **후보 출력 계산 전에** `EXP-CHOICE-CANDIDATES-003.json`을 고정한다. 이미 노출된 DEV에 대한 탐색이며 확인적 평가가 아니다. 더 좋은 수치가 나올 때까지 임계값·후보·자료를 바꾸지 않는다. 후보는 두 개다.

1. **작은 가치 차이 안의 레퍼토리 우선:** ensemble 최고 Q와 0.05 %p 이내인 행동 중 4월 TRAIN 레퍼토리 빈도가 가장 큰 구종을 고른다. 빈도가 같으면 Q, 그다음 원래 행동 index로 정한다. 0.05는 진단에 사전 지정했던 기술 요약 기준 중 하나이며 실제 무차이·실질적 동등성 임계값이라는 근거는 없다. 배합 집중도와 안정성이 나아져도 내부 Q 손실을 감추지 않는다.
2. **구성원별 최대 후회 최소화:** seed42–46 각각의 최고 Q 대비 손실을 %p로 구하고 그중 최대값이 가장 작은 행동을 고른다. 동률은 ensemble Q와 행동 index로 정한다. 서로 다른 seed의 절대 WE 수준을 직접 최솟값으로 섞지 않고 같은 seed 내 차이를 사용한다. 구성원 모두로 고른 후보를 같은 구성원으로 평가한 낙관성은 명시한다.

두 후보 모두 Q·데이터·지원 행동·미래 continuation은 바꾸지 않는 **현재 한 행동의 대안**이다. 저장 Q의 이후 경로는 평가 모델별 최적 continuation이므로 이 표를 후보 전체 타석 정책의 rollout 가치라고 부르지 않는다. 5개의 leave-one-member-out 계산을 추가해 나머지 4개 평균 Q를 쓰는 참고 선택과 후보의 보류 구성원 Q를 비교한다. 평균 Q는 확률 ensemble을 다시 푼 Q와 같지 않으므로 별도 민감도 통제군으로만 표시한다.

학습·추가 자료·CV·2026·최종셋 사용 없음. 같은 6경기·151타석/563구의 저장된 진단 Q와 TRAIN 레퍼토리만 읽는다. 행동 순서/지원 집합이 다르면 조용히 교집합으로 줄이지 않고 실패한다. 각 후보의 변화 공 수, 집중도, 일치율(설명용), 각 평가 모델의 현재 행동 차이, 최대 regret, leave-one-out 안정성과 보류 점수 및 경기 bootstrap CI를 남긴다.

**판정:** ensemble 현재 행동 평균 가치가 감소하면 내부 모형 기준 악화이며 서비스 채택에서 기각한다. 집중도/일치율/안정성 개선으로 덮지 않는다. 내부 비악화라도 식별 가능한 정책 평가가 없으면 보류다. 이번 입력에는 목표 행동/propensity가 없어 실제 정책 효과/OPE는 null이며 자동 채택 경로는 없다. 결과를 얻은 뒤 세 번째 후보를 추가하지 않는다.
