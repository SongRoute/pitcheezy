# 다음 세션 — 논문 기준 ML 예측 성능 실험

2026-09-24 사용자 우선순위 변경(D52). 이번 세션은 **계획·인수인계까지만 완료**하고 새 학습·평가는 다음 대화에서 시작한다. 실행 기준은 `ec84c89`와 이 문서를 포함한 문서 커밋이다. 이전 [ABCD 결과](ABCD-quality-v3.md)는 보존하며 그 실행 계획을 다시 시작하지 않는다.

## 1. 개발 순서와 이번 연구의 목표

1. 현재 고정한 지원 범위에서 ML 예측 성능을 개선한다.
2. 연구 후보를 고정한 뒤 지원 투수를 확대하고, 기존/신규 투수 성능과 지원률을 따로 재검증한다.
3. 실제 CV 산출물을 인수한 뒤 의도·제구·사건 기여 분석을 확장하고 검증한다.
4. 팬 이해 평가와 그 결과에 따른 UI/UX 개발을 진행한다.
5. 경기 공급·지연·복구를 검증해 실경기에 운영한다.

현재 집중 대상은 **투구 결과 확률을 잘 예측하는 ML 모형**이다. 실제 선택 구종 맞히기는 별도 과제이며 추천 품질의 정답으로 쓰지 않는다. 논문 기반 예측 모델의 연구 후보 선정에는 예측 지표를 사용한다. 이는 서비스 추천 정책 채택과 다르며, 정책 우위에는 별도 독립 정책 평가가 필요하다. 목표 위치의 효과는 실제 의도 자료 없이 검증 완료로 표시하지 않는다.

‘최대한’은 무제한 탐색을 뜻하지 않는다. 데이터·모델·튜닝 예산을 고정한 묶음마다 개선과 실패를 남기고, 추가 개선의 근거와 비용을 검토해 다음 묶음을 정한다. 예산 내 최고 관측 성능과 일반화가 확인된 성능을 구분한다.

## 2. 논문 기준선과 지표

원문 과제 재현표와 공통 과제 비교표를 분리한다. **논문 보고값은 참고 열**, 우리 데이터로 재실행한 값은 재현/적응 실험 열이다. 서로 다른 클래스·입력 시점·분할의 절대 점수는 순위를 매기지 않는다. 논문 발표가 2026년인 것은 2026 경기 데이터를 쓰는 것과 다르다.

| 기준 | 원문에서 확인한 예측 평가 | 이번 실험에 가져올 것 | 충실 재현 감사 항목 |
|---|---|---|---|
| SmartPitch, Otremba 2022 | §7.1.2–7.1.3, pp.73–76: 4개 결과의 categorical cross-entropy, Brier; 빈도·logistic regression·random forest·GBDT·신경망 비교 | 빈도/선형/신경망 기준선, NLL·Brier | 개인 타자 성향·존 정보, 실제 물리 입력, 2019 자료와 분할. 기존 B1 축소 MDP를 충실 재현으로 부르지 않음 |
| Melville et al. 2023 | §4.1, pp.11–13: 9개 결과 보정 곡선, cross-entropy, 타석 내 공 번호별 memoryless 절제 | 같은 입력의 비시퀀스/순환 모형, 공 번호별 성능·보정 | 2021–22 타석 무작위 75/25 분할과 우리 시간 분할의 차이, 현재 공 물리 입력, recurrent cell 정확한 구현 |
| Takamido & Nakamoto 2026 | §3.1 p.18: 2스트라이크 종결구 이진 분류 accuracy .756, precision .755, recall .879, F1 .812, ROC-AUC .811; 임계 .43 | Transformer와 이진 보조 과제의 같은 지표; 임계 선정은 CAL에서만 | 현재 공 포함 물리 시퀀스, positive label·혼동행렬 방향, 원문 임계 선정 방식, 2018–24 학습/2025 평가(2020 제외) |
| Douglas et al. 2021 | §6.1: 스윙 조건부 4개 결과의 예측/관측 빈도 비교; 제구 공분산 MSE는 별도 모듈 | 계층적 결과 모형의 참고 및 조건별 보정 비교 | 스윙 조건부 분모를 전체 투구와 혼동하지 않음. 제구 모듈 검증은 실제 의도 자료 단계로 분리 |

출처: [SmartPitch 공식 PDF](https://dspace.mit.edu/server/api/core/bitstreams/64b16c4f-a7e6-49a6-a7e5-806f9234f2a9/content), [Melville 공식 PDF](https://cdn.prod.website-files.com/68d6be744d7efccc2207f571/68d6be744d7efccc22080794_A%20Game%20Theoretical%20Approach%20to%20Optimal%20Pitch%20Sequencing.pdf), [Takamido 원문](https://arxiv.org/pdf/2606.17345), [Douglas 원문](https://arxiv.org/html/2110.04321v1). SmartPitch/Melville은 앞선 세션의 공식 PDF 추출본을 이번에 다시 확인했다. SmartPitch의 타자 성향 부재/원문 미열람이라는 오래된 설명은 사용하지 않는다.

다음 세션 첫 산출물 `docs/contracts/ML-BENCHMARK-v1.md`에 논문별 입력·정답·표본·분할·전처리·구조·손실·튜닝·지표 수식·코드 출처·이탈 목록을 적고, 대응 config와 함께 **후보 결과 전에 커밋**한다. 논문 원문/코드와 차이가 있으면 ‘논문 기반 적응’으로 표시한다. 없는 원자료를 보유했다고 가정하지 않으며, 이용 조건을 확인하지 않은 코드를 복제하지 않는다.

## 3. 공통 저울과 누수 방지

주 비교는 현재 서비스 계열의 **10개 투구 결과 분포**를 같은 적격 투구에서 예측한다. 기존 `src/pitcheezy`의 11-class RE24 실험 및 논문의 4/9/2-class 과제는 별도다. 레이블 매핑은 먼저 코드와 대조하고 누락·병합·제외 건수를 저장한다.

- 주지표: 투구 평균 NLL/cross-entropy. 보조: multiclass Brier(클래스 합, 범위 [0,2]), accuracy, macro-F1, 클래스별 precision/recall, 보정 곡선·ECE. 이진 과제는 ROC-AUC·PR-AUC도 남긴다. 클래스가 없으면 관련 지표는 null/이유를 기록한다.
- 경기·투수별 NLL, 구종·카운트·좌우·희귀 결과별 성능, 적격/지원/제외 분모를 함께 기록한다. 클래스가 많은 정답을 찍어 accuracy만 높이는 후보를 우수하다고 선정하지 않는다.
- 입력은 해당 판단 시점에 이용 가능한 상황·과거 기록과 평가할 후보 행동이다. 로그에 기록된 구종을 행동 조건으로 둔 결과 예측과 구종 자체 예측을 구별한다.
- 현재 공의 실제 구속·회전·도달 위치·타구/결과 정보는 투구 전 주 평가의 입력에 넣지 않는다. 조건부 물리 모형은 **TRAIN에서 얻은 동일 delivery 표본으로 적분**한다. 현재 실제 물리를 쓰는 원문 재현은 사후 조건부 예측 표에만 둔다.
- 데이터·정규화·범주/군집·빈도표·임베딩·타자 성향·최근 기록은 시점 경계를 지킨다. pitcher/batter 시즌 최종 집계나 뒤 경기 정보가 과거 예측에 섞이지 않는지 감사한다.
- TRAIN → 내부 시간 검증/조기 종료 → CAL(온도/혼합/분류 임계) → DEV → 최종 미열람 평가를 역할별로 분리한다. 경기와 타석이 경계에 걸리면 통째로 배정하며 purge 규칙을 명시한다. 시간 검증 밖에서 하이퍼파라미터를 선택하지 않는다.
- 기존 2024/2025 평가 구간과 July 2025 563구 진단은 이미 노출된 DEV다. 이름을 바꿔 새 holdout으로 취급하지 않는다. 두 연도 TRAIN/평가 중첩도 기록하고 독립 반복으로 합치지 않는다.
- **2026은 학습·튜닝·모델 선택 금지이며 이번 ML 연구에서는 추가 열람하지 않는다. 최종 평가셋도 개발에 쓰지 않는다.** 비2026 미열람 평가셋의 존재·노출 이력부터 메타데이터로 감사한다. 없으면 DEV 실험은 계속하되 ‘독립 일반화 확인 대기’로 남긴다.
- 현재 관측 후 지원/위치 결측 조건 때문에 제외된 표본이 있다는 점을 보존한다. 공통 교집합 점수와 전체 대상 지원률을 함께 보고, 후보가 어려운 표본을 버려 성능을 높이지 못하게 한다.

## 4. 기존 결과를 재사용할 출발점

| 기록 | 확인된 결과 | 다음 연구에 주는 제약 |
|---|---|---|
| [시간 분할·혼합 비교](../../experiments/pitchmdp/TEMPORAL_BLEND_RESULTS.md) | 2025 NLL: 빈도 1.505737, MLP+빈도 1.491226, Transformer+빈도 1.490642. Transformer−MLP 95% 구간은 0 포함; 2024도 우열 미확정 | 강한 기존 MLP와 Transformer를 함께 대조군으로 유지; 더 큰 모델이 항상 낫다고 가정하지 않음 |
| [타자·이력 절제](../../experiments/pitchmdp/REPRESENTATION_HISTORY_RESULTS.md), [0/5구 추가 검증](../../experiments/pitchmdp/HISTORY_BATTER_VALIDATION_RESULTS.md) | 연속 타자 성향에 근거가 있고, 이력 길이·군집 추가 이득은 일관되게 확인되지 않음 | 같은 실험을 새 가설 없이 반복하지 않음. 0/5구 단순 기준을 보존 |
| [ABCD 품질 결과](ABCD-quality-v3.md) | 추천 선택 두 후보 기각, 실제 정책 효과 미검증 | 이 실패를 새로운 backbone의 예측 가능성 부정으로 확대하지 않음 |

출발 연구 코호트는 기존 시간 분할 benchmark의 TRAIN 선정 6명을 재사용하는 안이다. 2025는 Webb(657277), Wheeler(554430), Gilbert(669302), Burnes(669203), Nola(605400), Berríos(621244). Burnes의 DEV 0건도 유지하며 성능 좋은 선수로 교체하지 않는다. 이는 현재 관전 서비스의 지원 명단과 동일하다는 뜻이 아니다. 다음 세션에 실제 동결 manifest와 대조해 두 명단을 분리 기록하고, 결과를 보기 전에 현재 연구 지원 범위를 고정한다.

과거 학습량은 2025 NN 276,821행/전체 TRAIN 1,252,824행이다. 151타석/563구 관전 진단만으로 모델 연구를 제한하지 않는다. 기존 기간·물리 적분·보정 규칙과 동일한 경우에만 기존 예측을 재사용하고, 달라지면 기준선도 공통 표본에서 다시 실행한다.

## 5. 실험 순서

| 묶음 | 변경 축과 후보 | 완료 산출물/진행 조건 |
|---|---|---|
| ML0 사양·재현 | 논문 충실도 감사, 데이터/노출 목록, 기존 빈도·MLP·Transformer 재현, 작은 CPU/MPS 비교 | 사전 고정 사양/config, metric 재계산 일치, 저장/복원·누수·확률합 검증, 시간·메모리 profile |
| ML1 학습 데이터 | 같은 backbone에서 학습량 25/50/100%의 중첩 경기 표본; 별도 비교로 최근 기간/누적 기간 또는 균등/최근 가중 | 고정 DEV의 학습 곡선. 학습량과 시점 구성을 한 비교에서 동시에 바꾸지 않음; 가중·정규화는 TRAIN만 사용 |
| ML2 backbone | 같은 데이터·정보에서 선형 기준, 기존 MLP/Transformer, GBDT, Melville 기반 recurrent 모델 | 모델별 원래/보정 후 점수·비용, 공정한 탐색 예산. GBDT는 SmartPitch의 비교 계열을 포함하되 설정 충실도는 별도 감사 |
| ML3 정보·학습 방식 | 오차 분석에서 근거가 나온 최대 2축: 예를 들어 경기 내 투수 변화/피로 또는 타석 간 과거 이력. 대안으로 direct pre-pitch head와 delivery 적분 방식 비교 | 결과 보기 전에 축·가설·대조군을 고정. 새 정보·구조·손실을 한꺼번에 바꾸지 않음 |
| ML4 결합·재검증 | 앞선 결과의 유망 조합 최대 2개, 제한된 HPO, 온도 보정·혼합, 5 seeds | 추가/제거 실험, 기존 강한 기준선 대비 불확실성·비용·회귀 검사, 독립 검토, 연구 후보 버전 동결 |
| ML5 지원 확대 | 연구 후보 고정 후 TRAIN 규칙으로 새로운 투수/역할/표본량 범위를 단계 확대 | 기존 투수와 신규 투수 점수 분리, 학습에서 본/못 본 투수 구분, 선수별 불확실성·지원률. 재튜닝이 필요하면 새 DEV 단계로 등록 |

첫 실행 세션의 필수 목표는 **ML0 완료 + ML1 첫 비교 묶음 실행·기록**이다. 독립적인 baseline 구현은 병렬 진행하되 무거운 실행은 순차로 한다. 성능 문제가 아닌 구현 오류는 고친 뒤 새 run ID로 재실행하고 실패 기록도 남긴다. 모든 backbone을 거대한 조합으로 한꺼번에 돌리지 않는다.

탐색은 seeds `{0,1,2}`, 최종 연구 후보는 `{0,1,2,3,4}`. 모델별 HPO 상한과 총 accelerator 시간은 ML0 profile 후 후보 결과 전에 고정한다. 기본 첫 screen은 모델당 대표 설정 1개이며, 후속 HPO는 모델당 최대 8설정으로 시작한다. trial 수와 실제 계산량을 모두 공개한다. 중간 성능을 보고 특정 모델만 무제한 튜닝하지 않는다.

짝지은 경기 bootstrap 95% 구간, seed별 결과와 앙상블 결과를 모두 기록한다. 여러 후보 중 고르는 묶음은 비교군 보정 또는 별도 선정/확인 분할을 미리 정한다. 고정 예측의 bootstrap은 학습·CAL·모델 선택 불확실성 전체를 반영하지 않는다고 표시한다.

연구 후보 선정 규칙은 ML0에서 최소 실용 개선폭과 Brier·희귀 결과·지원률·비용의 허용 범위를 먼저 수치로 정한다. 주 NLL 개선과 guardrail을 통과한 후보만 개선으로 분류한다. 점추정만 낮거나 구간이 겹치면 ‘우열 미확정’, 명확한 악화는 ‘기각’이다. 동률이면 더 단순하고 빠른 모델을 다음 연구 기본값으로 삼을 수 있으나 성능 우위라고 부르지 않는다. 사전 계획한 두 연속 묶음에서 추가 개선 근거가 없으면 현재 최선과 남은 불확실성을 정리하고 확대 단계 진입 여부를 판단한다.

## 6. 실험 기록과 재현 번들

기존 저장소 규칙을 사용한다. ID는 `docs/experiments.md`와 configs를 확인해 충돌 없는 `EXP-P{phase}-{seq}`를 발급한다. 한 ID는 대조군 대비 변경 한 축이며, 묶음의 부모 ID/후속 관계를 기록한다.

- `configs/{ID}.yaml`: 가설, 기준선, 데이터/분할/피처/레이블 버전, backbone, 학습·중단·튜닝 예산, seeds, 지표·선정 규칙.
- SSD의 새 `{ID}/{seed}/`: 원자료를 가리키는 manifest, checkpoint, 전처리/보정기, 투구별 확률·정답키, 로그·환경·해시. 원자료를 덮어쓰지 않는다.
- `results/{ID}.json` 및 보고서: 기준선과 차이/구간, 클래스·선수·기간별 결과, 지원/제외 분모, 실패/중단, 시간·peak 메모리, 개선/악화/미측정, 선정/기각 이유.
- `docs/experiments.md`: 성공·실패를 포함한 한 행 요약과 결과 링크. 표본·지표가 다른 실험을 한 순위표에 섞지 않는다.
- 실행 시각, Git commit/dirty 여부, data/config/checkpoint SHA256, 라이브러리·장치·seed·명령·종료 코드, 연구 후보와 실제 서비스 모델 버전을 남긴다.
- 독립 검토자는 저장 예측에서 지표를 재계산하고, 일부 체크포인트 추론도 다시 확인한다. 미래정보·분할·물리 적분·보정·선정 편향과 실패 실험 누락을 검토한다.

외부 실험 추적 서비스는 필수가 아니다. 우선 Git의 요약과 SSD의 큰 아티팩트로 재현 가능한 기록을 완성한다. 실제 자료/CV가 없는 칸을 개발 샘플로 채우지 않는다.

## 7. GPU 판단

이번 확인: Apple M4, 통합 메모리 24 GiB; `.venv-observer-standalone`의 PyTorch 2.14.0에서 MPS build/available=true, CUDA=false. 기존 같은 장비의 평균 fit은 MLP 22.7초, Transformer 158.3초였고, 이 수치는 전처리와 최종 400표본 적분 추론을 제외한다. 출처는 위 시간 분할 비교 보고서다.

**첫 묶음에 외부 GPU 서버는 당장 필요하지 않다.** 이것은 과거 규모에 근거한 판단이며 새 모델의 실행 시간 보장은 아니다. ML0에서 전처리·fit·보정·적분 추론·저장 시간을 나눠 측정하고, 모델/optimizer/batch/activation을 포함한 peak 메모리와 CPU fallback을 확인한다. GBDT나 파일 입출력 병목에는 GPU를 추가해도 개선이 제한될 수 있다.

로컬 예상 한 실행이 2시간을 넘거나 메모리/필수 연산 지원을 초과하면 기존 예비 Colab Pro 또는 단일 CUDA GPU 환경을 검토한다. 필요한 VRAM은 profile로 산출하고 여유를 더해 선택한다. 초기부터 다중 GPU 서버를 전제로 하지 않는다. 데이터 전송·환경 설치·저장·비용을 포함한 구체적인 실행안을 만든 뒤 외부 자원을 사용한다. 이번에는 서버 신청·유료 실행·새 학습을 하지 않았다.

## 8. Astra/Sol와 재귀 위임

사용자의 새 허가에 따라 다음 ML 연구에서 **재귀 위임을 허용**한다. 앞선 ABCD 묶음의 재귀 금지는 그 실행의 기록으로 보존한다. 현재 도구 한도는 **총 4개 동시 에이전트(주 에이전트 포함)**이며 다음 세션에도 재확인한다. 재귀로 한도를 우회하지 않는다.

| 역할 | 모델/기본 effort | 책임 |
|---|---|---|
| 총괄·연구 판단 | `gpt-6-astra`, medium; 복잡한 누수/통계/선정 검토는 high | 과제·분할·지표·실험 우선순위, 오류 원인, 연구 후보 선정 |
| 독립 검토 | `gpt-6-astra`, medium 또는 필요한 분석만 high | 구현 담당과 분리해 논문 충실도·누수·결과 해석 검토 |
| 구현·실험 실행 | `gpt-6-sol`, medium | 확정 사양 구현, 단일 실행 큐, 로그·체크포인트·환경 기록 |
| 자료/지표 정리·검사 | `gpt-6-sol`, medium | 근거 추출, manifest, 지표 재계산, 회귀 검사 |

이 역할 배정은 사용자의 제안에 따른 운영 선택이다. [OpenAI 모델 안내](https://developers.openai.com/api/docs/guides/latest-model)는 Astra를 높은 추론 능력이 필요한 작업, Sol을 강한 추론을 제공하는 선택지로 설명하지만 이 프로젝트에서 비용/품질 우위를 실측한 것은 아니다.

권장 동시 구성은 총괄 + Sol 2개 + 필요 시 Astra 검토자다. 독립 검토 시 다른 자리를 비워 자원을 배정한다. 재귀는 구체적 하위 작업과 남은 slot이 있을 때만 사용하고 기본 위임 깊이는 2단계로 제한해 시작한다. 자식도 전체 한도·동일 예산을 따른다. 사양/결과 문서·실행기·검증기는 worktree와 소유 파일을 분리하고 최소 맥락만 전달한다.

무거운 학습·평가 프로세스는 전체 트리에서 **한 번에 하나**다. 단일 실행 담당과 공통 실행 잠금을 사용한다. 자식이 임의로 추가 실험·데이터 범위·튜닝 예산을 늘리지 않으며 설계 변경은 총괄에 올린다. 연구 판단을 Sol 실행자에게 묵시적으로 넘기지 않는다.

새 대화 주 모델을 도구로 임의 변경했다고 주장하지 않는다. 가능한 경우 Astra로 시작하고, 주 모델을 확인할 수 없으면 중요한 판단을 명시적 Astra 하위 작업에 맡긴다. 요청한 모델 ID와 도구가 확인해 주는 실제 모델/effort를 구분해 기록하며, 노출되지 않는 토큰·비용은 ‘미측정’이다. 이번 계획 정리에는 새 하위 에이전트를 호출하지 않았다.

## 9. 보존 상태와 다음 대화 첫 지시

이번 마무리에서 8766/8767 `/api/health` 모두 ok, model/dataset ready, historical replay, CV no-media를 읽기 전용으로 확인했다. 원래 8766은 PID 94628/94642, 새 UI 8767은 PID 6713/6715였다. PID는 다음 시작 때 다시 확인한다. 기존 `C-D-FULL-001`과 `D-UI-003` DB·서버를 학습/검사에 재사용하거나 초기화하지 않는다. 새 ML 실행 공간을 별도로 만든다.

맥북 새 UI 확인은 `ssh -N -o ExitOnForwardFailure=yes -L 18767:127.0.0.1:8767 macmini` 후 `http://127.0.0.1:18767/`. 원래 화면의 18766 터널도 유지 가능하다. 두 저장 DB는 별개다.

현재 외부 의존은 실제 CV/검토 라벨, 당시 교체 가용성 근거, 팬 평가 참가자, 경기 공급·운영 조건이다. **초기 ML 실험은 이 입력을 기다리지 않는다.** 미열람 비2026 자료가 없다면 독립 최종 확인만 대기한다.

다음 대화에 붙일 지시문:

> /Users/song/Projects/pitcheezy에서 docs/SESSION_HANDOFF.md와 docs/handoffs/ML-experiments-next-session.md를 읽고 ML0부터 실제 실행해줘. 논문 기준선·평가 지표·데이터 시점과 노출 이력을 먼저 고정/커밋하고, 기존 결과 재현 뒤 ML1 첫 비교까지 진행해줘. 현재 연구 코호트에서 예측 성능 개선에 집중하고 지원 투수 확대·CV·UI/UX·운영은 후속 단계로 둬. 판단과 독립 검토는 Astra, 구현·실험 실행·기록은 Sol medium을 사용하고 재귀 위임을 허용해. 현재 동시 한도를 확인하고 전체 트리의 무거운 실행은 하나만 허용해. 결과·실패·비용·버전을 기록하고 원자료·동결 모형·runtime_src·사용자 서버/DB를 보존해. 2026과 최종셋은 개발·튜닝에 쓰지 말고, 예측 개선을 추천 정책 우위라고 해석하지 마. 외부 GPU가 필요하면 로컬 profile과 구체적인 실행안을 먼저 제시해.
