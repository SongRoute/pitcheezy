# 결정 로그

한 줄씩. `날짜 / 번호 / 결정 / 이유`. 뒤집을 때는 지우지 말고 새 줄을 추가하고 옛 줄에 "→ D{n}으로 대체" 표시.
노션 02 결정 로그(ADR-1~4)는 여기로 이관했고 이후 노션은 쓰지 않는다.

- 2026-09-07 **D1** 실험설계 노트(현 docs/design.md)를 기준 설계로 채택.
- 2026-09-07 **D2** 2026 검증셋은 정규시즌 종료 직후 1회 갱신(`--freeze`) 후 고정. 포스트시즌 제외.
- 2026-09-07 **D3** 전이 모델 비교의 NLL·ECE는 학습 창 내 홀드아웃(2023–24 학습 → 2025). 2026은 OPE·분해 전용. 이유: 정책이 자기 시뮬레이터로 자기를 평가하는 순환을 끊는다.
- 2026-09-08 **D4** 인터페이스 스펙 v1 채택. 상태 288×K, 행동 225, 결과 11, float32, 마스크 3층, npy 디렉터리 저장. docs/interface-spec.md.
- 2026-09-15 **D5** 개인 개발로 전환. 트랙 A/B/C, 노션 SSOT, ADR 템플릿, 스펙 태그 절차, 리뷰어 요건 폐지. 레포가 유일한 출처. 이유: 협업 조정용 규칙은 혼자면 비용만 남는다. 과학적 타당성 규칙 5개(CLAUDE.md 절대 규칙)는 유지.
- 2026-09-15 **D6** 목표를 "기존 연구 대비 개선"으로 명시. 비교 대상 = SmartPitch(Otremba 2022), Takamido & Nakamoto(반사실 시퀀스 최적화). 같은 데이터 버전·같은 2025 홀드아웃·같은 2026 OPE에서 재구현해 비교. 논문 보고 수치는 우리 표에 옮기지 않는다. docs/baselines.md.
- 2026-09-15 **D7** 4자 책임 분해는 Phase 2 후순위. 설계(design.md §0-f, ⑥, ⑤)는 유지하되 추천 성능 확인 뒤 진행.
- 2026-09-15 **D8** Phase 1 첫 개선 축 = "직전 N구 시퀀스 맥락" (Takamido에서 착안, design.md §1-e 맥락 피처 후보에 이미 있던 항목). 스펙 v1의 context 축 삽입 규칙으로 처리. 이어서 xRE24 + 결과 공간 세분화, 투수별 구조 ⓑⓒ 순.
- 2026-09-15 **D9** 실험 ID에서 트랙 문자를 뺀다: `EXP-P{phase}-{seq}`. configs/는 평탄화 (`configs/{ID}.yaml`).
- 2026-09-15 **D10** 맥미니(M4, 24GB, 상시 가동)가 주 실행기. Colab Pro는 로컬 예상 2시간 초과 또는 메모리 24GB 초과 실험에만. 이유: Phase 0(투수별 집계·VI·IPS)과 B2 Transformer(표본 10만, 소형)는 로컬 CPU/MPS로 충분하고, Colab 세션 만료 문제가 사라진다. 데이터 원본은 Drive에 두고 맥미니에 동기화, 해시로 검증.
- 2026-09-15 **D11** 하루 루틴: 아침 새 세션 → 밤사이 실험 결과 확인(results/, runs/_logs/) → experiments.md 갱신 → 다음 한 가지 결정 → 진행 → 저녁에 다음 밤 실험을 걸어두고 종료. 실험은 Claude 세션 밖(tmux `runs` 창, nohup)에서 돌린다. 스크립트 scripts/dev.sh.
- 2026-09-17 **D12** 포스트시즌은 2026 freeze 홀드아웃에서 제외(D2 유지)하되, P0 결과표 이후 별도 "데모 슬라이스"로만 허용: 이미 학습된 레귤러 선발 풀에서 포스트시즌 진출팀 투수를 뽑아 포스트시즌 투구에 OPE를 한 번 더 돌린다. 채택 판단에는 쓰지 않는다. 이유: 투수별 모델(ⓐ)이라 학습 범위를 진출팀으로 줄여도 얻는 것이 없고, 타자 군집·RE24는 전체 데이터가 필요하며, 포스트시즌은 표본(~1만 구)이 작고 분포가 다르다. 필요 작업: fetch_data.py 포스트시즌 수집 옵션(10월 중순 이후).
- 2026-09-17 **D13** 인터페이스 스펙 v1 코드화 완료 (`src/pitcheezy/interfaces/`, 계약 테스트 101개). 규칙 마스크는 "3볼에 볼 불가·2스트에 스트라이크 불가"의 뒷면(BB 는 3볼, K 는 2스트에서만)까지 포함. Statcast 매핑은 종결=events, 비종결=description 으로 판정하고 피치클록 자동 볼/스트라이크는 카운트만 진행. 스키마 변경 없음.
- 2026-09-17 **D14** RE24·dRE24 표는 `data/re24/{re24_version}/` 에 커밋한다 (작고 모든 실험이 공유). 버전 = `re24-{시즌창}-{데이터 버전}-{계산 커밋}`. 계산 규칙: RE24 = 타석 시작 상태별 이닝 끝까지 득점 평균, dRE24 = 종결 투구 시점 상태 기준 RE[다음]+득점−RE[현재] 평균, 경기 마지막 하프이닝(끝내기 등 3아웃 전 종료 가능)은 제외. 첫 버전 re24-2023_2025-d20260911-s2325-82b008b (타석 521,935, dRE24 최소 셀 n=6).
- 2026-09-17 **D15** 레귤러 선발 풀 = 학습 창(2023–25) 안 어느 시즌이든 100이닝 이상인 투수, 그 투수의 창 안 모든 정규시즌 투구 사용 (`configs/pitchers_ip100.yaml`, `data/pitchers/pool-ip100-2023_2025-d20260911-s2325-2b19cc1`). 이닝 = 타석 사이 아웃 증가분 귀속 / 3 (Webb 2024 = 204.67, 공식 204⅔ 일치). 실측: 100이닝이면 시즌 126~128명·풀 219명·투구 110만 구로 design.md 의 "60~70명" 추정과 다르다. 그 인원은 150이닝(시즌 58~71명, 풀 130명)에 해당. 하한은 P0 결과 뒤 재검토 가능, 표는 table.md 에 있음.
- 2026-09-17 **D16 (잠정)** 전이 텐서 희소 셀은 계층 디리클레 평활: league(count) → league(count,action) → league(state,action) / league(count,구종) → pitcher(count,구종) → pitcher(state,action) (투수 편차를 비율로 곱함). 평활 강도 α 는 2025 홀드아웃 NLL 로 그리드 스크리닝(auto). 이유: 투수당 64,800 셀에 ~5천 구라 순수 집계는 거의 전부 빈 행. 첫 실행에서 league(count,action) 층을 넣자 NLL 1.29→1.22.
- 2026-09-17 **D17 (잠정)** 평가 정책 완화의 주 온도 τ=0.02 (RE24 단위). 상태별 Q 폭(max−mean 중앙값 0.07, 1위−2위 0.005)에 맞춤. 메뉴(greedy·topk·softmax·tilt 여러 τ)를 모든 실험에서 함께 기록하고 표에는 주 항목만.
- 2026-09-17 **D18 (잠정)** π_b 는 2026 로그에서 인수분해 계층 추정 π(구종|s,투수)×π(위치|구종,카운트,투수), 경기 단위 5폴드 교차 적합. 이유: 같은 데이터로 맞추면 로그 행동 확률이 부풀어 가중치 평균 0.05(이론 1)로 붕괴했고, 225행동을 한 축으로 평활하면 희귀 구종이 바닥 확률로 떨어져 비가 폭발했다.
- 2026-09-17 **D19 (잠정)** P0 OPE 저울 = 1스텝 편차 SNIPS (한 투구만 π_e, 나머지 π_b; 투구 비 클립 20; 공통 지지 = 학습 레퍼토리 ∩ 2026 에서 20구 이상 던진 구종). 궤적 IPS·SNIPS 는 설계상 e2e 저울이지만 225행동×타석당 ~4결정에서 ESS≈0 이라 기록만 한다 (tilt 정책은 ESS 1~2%). 경기 클러스터 부트스트랩 95% CI. 재검토: 궤적 저울을 살릴 방법(DR, 거친 행동 공간)은 P1 과제.
- 2026-09-17 **D20 (잠정)** 설계 ③ 보수화 축 "π_b KL 제약"을 P0 로 앞당겨 주 정책으로: π_e ∝ π_b·exp(Q/τ) (tilt). 이유: softmax(Q/τ)만으로는 노이즈 Q 가 π_b 가 안 던지는 셀에 질량을 줘 OPE 불가. tilt 는 "투수 자기 레퍼토리 안에서 모델 방향으로 조정"이라 배포 의미도 분명. 모델 내 가치(model_value)와 OPE 차이를 착취 폭 진단으로 함께 기록.
- 2026-09-17 **D21 (잠정)** 타자 군집 v0 = (타자, stand) 단위 k-means, 좌우 3개씩 K=6, 특징 10개(K%·BB%·스윙·체이스·컨택트·땅볼·뜬공·강타·컨택트 xwOBA·평균 발사각), 학습 창 100타석 미만·2026 신규 타자는 리그 평균에 가장 가까운 군집. `data/batters/clusters-K6-…` 커밋. K>1 실험은 홀드아웃 텐서(3.7GB)를 저장하지 않고 지표·meta 만 남긴다 (Drive 용량). 전체 텐서는 저장.
- 2026-09-17 **D22 (잠정)** OPE 판정은 같은 평가 타석에 두 정책을 대입한 짝지은 차이 Δ 의 경기 클러스터 부트스트랩 95% CI 로 한다 (`scripts/ope_compare.py`). 따로 잰 CI 의 겹침은 검정력이 낮아 0.002 규모 차이를 못 가른다. 첫 적용: EXP-P0-009 > B1(002) Δ +0.0026 [+0.0007, +0.0044].
- 2026-09-18 **D23** D16~D22 잠정 결정을 확정. 조건: 보고서에서 1스텝 편차 SNIPS 는 "정책 개선 방향의 지표"이지 시즌 전체 효과 추정치가 아니라고 명시. 궤적 OPE 를 살리는 시도(DR 또는 거친 행동 공간)는 P1 에서 하나 한다. α 선택은 계속 홀드아웃 NLL(2026 을 튜닝에 쓰지 않음).
- 2026-09-18 **D24** D8 순서 변경: P1 첫 축 = 투수별 구조 ⓑ(공유 모델 + 투수 임베딩). 이유: 위약 검사에서 다른 투수의 Q 로 기울여도 거의 같은 이득(001: +0.031/+0.031) → 현재 모델은 리그 공통 지도이고 투수별 정보가 없다. 006/009 에서 투수 층 해상도를 올리자 처음 갈렸다(+0.038/+0.033, +0.040/+0.035). 시퀀스 맥락은 B2 재구현과 함께 W5~W6 로.
- 2026-09-18 **D25** 선발 이닝 하한은 실험으로 정한다: 009 구조를 150이닝 풀(130명)에서 B1 과 짝지어 비교(EXP-P0-011/012). 차이가 유지되면 100 유지(표본 큼), 100 풀에서만 유의하면 스윙맨 영향 검토.
- 2026-09-18 **D26** 선발 이닝 하한 100 유지. 150 풀(130명, 타석 47,638)에서 009 구조 − B1 짝지은 Δ +0.0018 [-0.0004, +0.0041]: 100 풀(+0.0026)과 같은 방향·규모, 표본 감소로 유의성만 사라짐. EXP-P0-011/012.
- 2026-09-18 **D27** EXP-P0-009(투수층 구종×구역 + 군집 K=6 + 주자·아웃 보상)를 P0 우리 모델로 채택. B1 대비 짝지은 Δ +0.0026 [+0.0007, +0.0045], 5시드, 부트스트랩 시드 3개 모두 0 제외. 001 대비는 경계(+0.0022 [−0.0000, +0.0045]). 2026 freeze 뒤 재확인 필수.
- 2026-09-18 **D28** 일정은 상한으로만 쓴다: 10/31 마감·주차표보다 빨리 끝나면 끝내고 다음 축으로 넘어간다(애자일). 2026 freeze(9/27 이후)를 기다리지 않고 현재 수집본(d20260911)으로 P1 개발·짝지은 OPE 판정을 계속한다. freeze 뒤에는 채택된 실험(009 와 P1 채택분)을 고정본으로 재실행해 재확인하고, 뒤집히면 그때 행을 고친다. 이유: 시즌 종료까지 9일을 비워 둘 이유가 없고, 현재 수집본은 2026-09-09 경기까지라(data/versions.md) 그 뒤 경기는 어느 판정에도 쓰이지 않았고, 재확인 때 새 표본 구실을 한다.
- 2026-09-18 **D29** P1 첫 축(투수별 구조 ⓑ, EXP-P1-001~003) 결과: 공유 신경망 + 투수 임베딩은 홀드아웃 NLL 을 1.2100(006)→1.176 으로 크게 낮추지만, 2026 짝지은 OPE 는 006 과 세 시드 모두 구분되지 않는다(Δ +0.0000/−0.0003/−0.0006, CI 반폭 ~0.0024) → 채택 규칙(OPE 로만)에 따라 기각, P0 채택 모델(009)은 그대로. 코드는 `transition.arch: neural` 옵션으로 남긴다. 함께 정한 것: (1) 난수 있는 모델은 학습 시드마다 짝지은 비교, 세 시드 모두 0 제외여야 판정. (2) neural 은 시드별 디렉터리에 저장하고 시드 ≠ 0 은 OPE 뒤 P.npy 만 삭제(valid·Q 는 남김). (3) 규칙 마스크 위반 결과가 찍힌 학습 행(시즌당 2~5구)은 neural 학습에서 제외. 해석: 전이 모델의 예측력은 올랐는데 저울이 움직이지 않는다 — 현재 저울(tilt τ=0.02 의 1스텝 SNIPS)이 0.002 아래 차이를 못 가르는 것이 병목일 수 있다. 다음 축 선택은 미정(사용자 판단).
- 2026-09-20 **D30** 고감도 τ 는 저울 해상도를 올리지 않는다 → 주 저울 tilt τ=0.02 유지. 사전 등록 기준(메뉴 중 ESS ≥ 30% 인 가장 작은 τ = 0.005)으로 5개 짝을 재측정: Δ 는 약 2배, CI 반폭은 2.5배 → Δ/반폭 1.4→1.3. 유의했던 짝(006−002, P1-001−002)이 τ=0.005 에서 유의하지 않음. `ope_compare.py --policy` 로 재현 가능. 보고서 docs/reports/autorun-2026-09-20.md 판단 1.
- 2026-09-20 **D31** D23 의 "궤적 OPE 살리기(DR)" 시도 결과: 실패로 기록, 주 저울 유지. 1스텝 DR(π_b 연속가치 제어변량)은 SNIPS 와 CI 폭 동일(ESS 60% 라 줄일 분산 없음). 궤적 DR/WDR 은 궤적 SNIPS 대비 CI 폭 절반이지만 1스텝의 4배이고, 값이 모델 내 가치(직접법 항)에 끌려 §0-e 순환 평가로 되돌아간다(P1-001−002 Δ +0.05 는 모델 자기 평가 차이). `onestep_dr`·`traj_dr` 는 모든 실험에 보조 열로 기록만. 해상도 한계(짝지은 Δ 반폭 ≈ 0.001~0.0025/결정)는 2026 표본 크기의 한계. 판단 2, CHECKPOINT 1.
- 2026-09-20 **D32** 시퀀스 맥락 축은 별도 5차원 축이 아니라 state id 뒤에 접는다: `state_id' = state_id(c,b,k,K)·C + ctx`, S = 288·K·C (C=1 이면 v1 과 동일). 다음 상태가 행동에 의존하므로 `next_state_table` 은 C>1 에서 [S,A,O]. 맥락 v0 = 직전 구 구종 계열 4 (없음/속구계 FF·SI·FC/변화구계 SL·ST·CU/오프스피드 CH·FS·OT), 직전 구가 행동 제외면 없음. 이유: 4차원 텐서 소비자(VI·OPE·π_b·검증·저장) 무변경, C=10(구종 9+없음)은 K=1 에서 P 6.2GB 로 RSS 24GB 초과 추정. 스펙 v1.1. config `state.context: {kind: prev_pitch_family, C: 4}`.
- 2026-09-20 **D33** P1 두 번째 축(시퀀스 맥락 v0, EXP-P1-004/005) 결과: 기각. 홀드아웃 NLL 은 count(1.2104 vs 006 1.2100)·neural(1.1760/1.1755/1.1750 vs P1-001 1.1761/1.1756/1.1751) 모두 불변, 2026 짝지은 OPE 도 미구분(004−006 +0.0004 [−0.0007, +0.0015]; 005−P1-001 세 시드 −0.0006/+0.0007/+0.0003). 단서: 2스트라이크 슬라이스에서 005 가 006·P1-001 대비 세 시드 모두 양(+0.0015~+0.0043, 미유의) → 결정구에서만 맥락이 작동할 가능성. 후속 후보(미결): 맥락 v1(직전 구 계열 × 구역, C≈10~13)은 메모리 설계 필요. 코드는 `state.context` 옵션으로 남긴다.
- 2026-09-20 **D34** B2(Takamido류) 재구현 1단계(모듈층) 결과: 원문 구조는 2025 홀드아웃 NLL 1.0614 로 우리 006(1.2099)·P1-001(1.176)보다 크게 낮지만, 절제(EXP-P1-007/008)로 격차 0.148 을 분해하면 연속 물리 피처 0.108 / 6구 시퀀스 창 0.011 / 맥락 피처·공유 신경망 0.030. 연속 물리 피처는 정책이 고를 수 없는(셀 안 위치·그날 구속 = 커맨드 결과) 정보라 정책 비교 근거로 쓰지 않는다 — 비교표에는 "정보량 맞춘" 007 을 우리 모델 옆에 둔다. 시퀀스 창 몫(0.011)은 투수 임베딩(0.013, D29)과 같은 규모. 정책층(2스트라이크 argmin OPE)은 미구현. 구현 `src/pitcheezy/baselines/b2.py`, `scripts/run_b2_module.py`, 이탈 목록 docs/baselines.md.
- 2026-09-20 **D35** 시퀀스 맥락 v1 = 직전 구 구종 계열 × 존 안/밖 (`prev_pitch_family_zone`, C=7: 없음 / 계열 3 × {존 밖, 존 안}; 존 안 = 5×5 격자의 가운데 3×3). 인코딩은 D32 그대로. 이유: v0(계열만)이 NLL·OPE 를 못 움직였고 B2 절제(D34)에서 직전 구의 위치를 포함한 창만 0.011 을 줬다. 계획의 "이번에 안 하는 것"에 있던 축이지만 CHECKPOINT 2 의 추천 선택이라 사용자 지시("추천대로 끝까지 진행")에 따라 당겨 실행. 메모리는 `validate_transition` 청크화(float64 전체 복사 제거)로 RSS 14GB 에 맞춤. 스펙 v1.2.
- 2026-09-20 **D36** 맥락 v1(EXP-P1-009/010) 판정: 규칙상 기각, 단 "경계" 로 기록. count 009 − 006 짝지은 Δ +0.0011 [−0.0002, +0.0024] (부트스트랩 시드 3개 모두 하한이 0 을 살짝 포함), neural 010 − P1-001 은 시드 2 가 0 아래(+0.0011/+0.0010/−0.0001). B1 대비는 009 +0.0033 [+0.0013, +0.0054] 로 지금까지 가장 큰 유의 차이. 2스트라이크 슬라이스에서 v1 > v0 유의(+0.0025 [+0.0006, +0.0044]). 홀드아웃 NLL 은 불변 → 예측력이 아니라 정책의 행동 선택이 달라져 생긴 차이. 다음 단계 후보: (1) 채택 구조(K=6) 위에 v1 — 텐서 26GB 라 float16/유효 행만 저장 같은 메모리 설계 필요, (2) 2026 freeze 뒤 표본 +10% 에서 재확인. 사용자 판단(CHECKPOINT 3).
- 2026-09-20 **D37** CHECKPOINT 2·3 답(사용자): 텐서 저장 설계 먼저 → K=6 × 맥락 v1. 설계는 "valid 행만/float16 압축"이 아니라 **스트리밍 fit + 디스크 memmap(포맷 불변)**. 이유: (1) 실측 valid 비율은 57.3%(EXP-P0-009·P1-009 의 valid.npy, 투수당 valid 행동 50~200·중앙 125)로 보고서의 "~30%"(미측정)와 달라 valid 행만 저장해도 26GB→15GB(1.7배)뿐, (2) float16 은 행 합 1±1e-5 계약을 깬다, (3) 병목은 저장 포맷이 아니라 `count.fit` 이 dense n(int16 13GB)·P(26GB)를 RAM 에 올리는 것이고 소비자(VI·DR·검증)는 이미 투수 16명 청크로 mmap 을 읽는다. 구현: `smoothing.LeagueLayers`+`pitcher_layer`(값은 `smooth_hierarchical` 과 동일), `count.fit(out_dir=)` 이 투수 한 명씩 세고 평활해 `P.npy`·`n_obs.npy` memmap 에 바로 씀, `TransitionTensor.save` 는 제자리 memmap 이면 flush 만, α 스크리닝은 후보마다 `_fit_scratch/` 에 쓰고 최선만 남김. 검증: EXP-P1-009 전체 텐서를 새 경로로 재생성 → P.npy·n_obs.npy sha256 이 저장본과 일치, fit 46초·최대 RSS 5.3GB. 스키마 변경 없음. 남은 벽: 가치·OPE 단계의 [P,S,A] 배열(K=6×C=7 에서 float64 하나가 4.8GB) → 투수 블록 단위 실행으로 이어서 처리.
- 2026-09-20 **D38** 가치·OPE 단계를 투수 블록 단위로 실행한다 (D37 의 남은 벽). VI·정책 평가·π_b·tilt·q̂ 는 투수끼리 독립이고 추정량에는 로그된 투구 행의 값만 들어가므로, `ope/blocks.py::compute_rows` 가 투수 블록마다 [B,S,A] 를 만들어 행 단위 벡터(π_b·π_e 로그 확률, V_e, q̂_b, q̂_e, v̂_e_b)만 남긴다. `stage_value` 는 블록마다 VI 해서 Q·policy 를 memmap 에 바로 쓰고, `validate_value` 는 청크로 돈다. 블록 크기 = `block_size(S)`: P 블록 float64 ≤ 1GB, 상한 16 → 지금까지의 실험 크기(S ≤ 2016)는 16(= 기존 VI chunk)이라 값 불변, K=6×C=7(S=12096)은 4. 행 단위 재료는 부트스트랩 시드와 무관하므로 `s0/ope_rows.npz` 에 캐시(key = OPE 설정·메뉴·전이/가치 해시)해 시드 1·2 는 부트스트랩만 다시 한다. `ope_compare.py` 도 같은 함수를 쓴다(단일 출처). 검증: EXP-P1-009 s0 을 새 경로로 전 단계 재실행 → P·valid·n_obs·Q·V·policy sha256 전부 일치, ope/summary.json 전 항목 차이 0, 최대 메모리 footprint 4.2GB · 최대 RSS 9.8GB(파일 매핑 페이지 포함. 기존 기록 RSS 14.4GB), 시간 전이 82→165초(T7 memmap 쓰기)·가치 155→155초·OPE 589→560초. 한계: `ope_placebo.py` 는 아직 통짜 [P,S,A] 경로라 K=6×C=7 에서는 못 돈다.
- 2026-09-20 **D39** EXP-P1-011(채택 구조 009 + 맥락 v1, K=6×C=7) 판정: 기각 — 009 와 구분되지 않는다(짝지은 Δ +0.0004 [−0.0004, +0.0011], 부트스트랩 시드 3개 동일). K=1 위 v1(P1-009) 대비 −0.0004 [−0.0015, +0.0008], 2스트라이크 슬라이스도 미구분(vs 009 +0.0006, vs P1-009 −0.0015 [−0.0034, +0.0004]). B1 대비는 +0.0030 [+0.0011, +0.0048] 로 유의(우리 모델 > B1 재확인). 홀드아웃 NLL 1.2061(009 는 1.2053) 불변. 해석: D36 의 경계 신호(+0.0011)는 군집과 쌓이지 않았다 — 상태를 6×7 로 쪼개며 셀당 표본이 얇아진 것과 일치하지만 원인은 미검증. 채택 모델은 EXP-P0-009 그대로. 맥락 v1 은 freeze 뒤 K=1(P1-009 vs 006) 재측정만 남기고 이 축은 닫는다. 다음 축 후보는 xRE24(보상 변경, design.md §0-d A) — 사용자 판단. 인프라(D37·D38)는 S=12096 을 91분·RSS 17.6GB 로 돌려 목적 달성.
- 2026-09-21 **D40** 사용자 우선순위 변경: 실제 사용자 배포를 고려한 **전체 서비스 설계 → 모듈 설계 → 전 모듈의 가벼운 end-to-end 구현 → 개별 성능 향상** 순으로 진행한다. 구종·목표 위치 추천 성능의 연구·논문화는 제품 안의 후속 목표다. CV·위치·설명 모듈도 초기 통합 범위로 당긴다. 이는 개발 우선순위 확정이며 기술 스택·세부 모델은 아직 논의 초안이다. `docs/SERVICE_ARCHITECTURE_DRAFT.md` 참조. 기존 동결 실험·자료 승인 범위·미검증 효과 표시 원칙은 유지한다.
- 2026-09-21 **D41** 사용자가 전체 서비스 설계에 동의하고 CV 사용 범위를 수정했다: 다음 구종·위치 추천 → 실제 도달 위치 비교 → 타석 마지막 공 또는 가장 중요한 투구만 CV 사후 분석. 모든 투구의 CV를 추천 선행조건으로 두지 않는다. 운영 기본안은 타석당 최대1구·중복 캐시. 2026-09-22 06:00 KST까지의 야간 MVP 계획을 우선 작성하고, 개발 중 사용자 의견이 필요한 사항은 체크포인트로 기록해 독립 작업을 계속한다. 세부 계획은 docs/OVERNIGHT_MVP_PLAN_20260921.md. 사용자 답변으로 로컬 브라우저 MVP, 준비된 영상 없음(CV 연결·대체 입력까지), 기존 실험 환경을 보존한 별도 개발환경 패키지 설치 허용을 확정했다.
- 2026-09-21 **D42** 사용자 승인에 따라 `apps/observer` 로컬 관전 MVP를 구현했다. 실제3경기·24타석·78구의 사전 구종×9구역 추천→실제 위치 비교→마지막1구 분석 작업·수동 목표 입력 흐름, SQLite 지속 상태·별도 워커를 연결했다. 위치는 동결5-seed 모델의 TRAIN joint delivery 재가중 근사(σ0.45ft, 모든 카운트 ESS≥20·kernel mass≥0.01)이며 실제 의도 개입·정책 효과는 미검증이다. 영상 부재는 no-media 어댑터로 처리하며 CV 성공을 가장하지 않는다. PostgreSQL 실행환경 부재는 SQLite로 대체했다. 별도 웹 환경은 기존 과학 패키지를 읽기 전용으로 재사용한다. 백엔드15테스트·실데이터78구·브라우저 데스크톱/390px 흐름 검증 통과. 구현·검증은 `apps/observer/RESULTS.md`, 미결은 `apps/observer/CHECKPOINTS.md`. 기존 모델 핵심8파일 해시 일치를 확인했다.
- 2026-09-22 **D43** 사용자가 최종 목표를 확정했다: **야구 팬이 경기를 보며 참고하고 신뢰할 수 있는 웹페이지 하나.** 화면이 하는 일은 (1) 다음 공의 이상적인 구종·위치, (2) 실제 투구의 구종·위치 확인과 비교, (3) 홈런·삼진 등 중요 상황에서 원인 비중(타자·투수·작전/교체)을 수치로 제시. 이 과정에서 만드는 추천 모델은 기존 연구보다 좋은 성능을 내며, 방식은 "결과에 영향을 주는 요소를 더 많이 학습에 넣는 것"이다. 이에 따라 **D7(4자 분해는 Phase 2 후순위)을 대체한다** — 분해는 기능 3 자체이므로 최종 목표의 3분의 1이다. D40(서비스 우선)은 유지하되 목표가 구체화됐다. 따름 결과: (a) 의도(목표 위치) 기준점이 없으면 분해가 불가능하므로(design §0-f) CV·사람 검토 목표 라벨이 선택 기능이 아니라 선행조건이 된다, (b) "신뢰"가 정확도와 같은 급의 요구사항이 된다(근거 제시·보정·미지원의 정직한 표시), (c) 벤치·교체 성분은 단위가 WE라 RE24 분해에 섞지 않고 별도 축으로 설계한다. 채택 판정은 여전히 e2e OPE 로만 한다. 단계·통과 기준은 docs/roadmap.md.
- 2026-09-22 **D44** 일정을 목표 관리에서 뺀다. 최종 발표(11/24)·자체 마감(10/31)·P2 컷라인(10/19)·주차표는 더 이상 계획의 기준이 아니다. 개발을 의존성 순서로 진행하고, 발표가 필요한 시점에 그때까지 된 만큼 발표한다. 이유: 사용자 지시. D28(일정은 상한)의 연장선이며 상한마저 지금은 보지 않는다는 뜻이다. 예외는 2026 freeze(9/27 이후) 하나이고, 이것은 일정이 아니라 정규시즌 종료라는 데이터 사건이다(D2). 단계·의존성은 docs/roadmap.md.
- 2026-09-22 **D45** 같은 날 병렬 세션이 각각 만든 로드맵 두 개(`docs/GOAL_ROADMAP.md`, `docs/roadmap.md`)를 `docs/roadmap.md` 하나로 합친다. 본문은 사용자와 함께 만든 GOAL_ROADMAP(서브목표 G1~G7, 단계 M0~M6, 통과 기준)을 그대로 쓰고, roadmap 쪽에서 D44(일정 관리 중단)·의존성 순서·다음 한 가지·위험 셋을 흡수했다. GOAL_ROADMAP 의 "5. 일정과 우선순위"(10/31·11/24·10/19 앵커)는 D44 로 대체했다. 합치기 전 원본은 커밋 9b11230 에 보존. 이유: 레포가 유일한 출처인데 목표 문서가 둘이면 어느 쪽이 기준인지 갈린다. 함께 드러난 미결: GOAL_ROADMAP M0 은 기본 문제를 승리 확률(WE)로 두자고 하는데 design.md §0-c·§0-d B 는 학습 보상을 RE24 로 고정하고 WE 를 기각했다 — 두 트랙의 수치를 같은 표에 섞기 전에 결정 한 줄이 필요하다(미결, roadmap §1 에 기록).
- 2026-09-22 **D46** 의도(목표 위치) 영상 모듈은 별도 담당자가 맡는다(사용자 결정). 따라서 이쪽은 라벨을 모으는 대신 **입출력 계약을 먼저 고정하고 나중에 산출물을 합친다**. 경계는 `validate_intent_estimate` 하나이고 여기를 통과하지 못한 산출물은 저장하지 않는다. 계약 `docs/intent-spec.md`, 코드 `apps/observer/backend/observer_app/intent.py`·`intent_store.py`, 적합성 검사 `apps/observer/backend/tests/test_intent.py`(28개). 라벨 목표 개수·표본 설계·검토자 배정은 미정으로 남긴다(사용자 "선호 없음", roadmap M1 대로 첫 라벨의 일치도와 오차 폭을 재고 정한다). 기존 동결 모델·실험 산출물·해시와 `store.finish` 의 CV 성공 차단 장치는 건드리지 않았다.
- 2026-09-22 **D47** 의도 좌표계를 네 프레임 사슬로 고정한다: `image_pixels → annotated_image_zone → plate_feet → zone9`. 건너뛰기 금지, 홉마다 `version`·`error`·`error_status` 를 남기고 `error=None` 은 미측정이며 0 으로 대신하지 않는다. ①은 구현 있음(오차 미측정), ③은 구현 있음(양자화라 오차 대신 `boundary_margin_feet`), **②(annotated_image_zone→plate_feet)는 구현·측정 모두 없어 지금은 어떤 경로로도 9구역 의도가 나오지 않는다** — 담당자가 채울 단 하나의 항목이고 G4 의 병목이다. 홉이 없으면 도달한 프레임까지만 내고 `blocked_by` 에 이유 이름을 붙인다. 좌우 뒤집힘은 추론하지 않고 `x_convention=statcast_plate_x_catcher_view` 만 받는다(`CONTRACT.md` 의 `GET /api/zones`·`domain.py::ZONES` 와 같은 규약). 미트-공 차이는 같은 프레임에서만 빼고, `is_command_error=false` 와 아직 분리되지 않은 성분 다섯 개의 이름을 함께 낸다.
- 2026-09-22 **D48** 검토 라벨의 정의: `IntentEstimate` 는 항상 `review_status='unreviewed'` 이며 검토 상태를 스스로 주장할 수 없다. 검토 라벨은 `reviewer_id`·`decision(accepted/corrected/rejected)`·`reviewed_at`·불확실성(미측정이면 `value=null` + `basis` 명시)·근거 프레임이 실린 **별도 타입·별도 표**의 레코드이고, 둘 사이에 승격 함수를 두지 않는다. `corrected` 만 자기 좌표를 들고, `rejected` 는 검토 라벨을 만들지 않는다. 한 추정에 검토자 여러 명이 붙을 수 있다(일치도 측정용). 실측 2026-09-22: 저장 라벨 5개(구간 4 + 검증 중복 1) 전부 AI 추정·미검토, `calibration_corners` 전부 없음이라 모두 `image_pixels` 에서 멈춤, `pitch_id` 연결 0개, 추적 결과 4건 전부 기권, **사람 검토 라벨 0개·9구역 의도 0개**. 표본은 한 투수·한 경기·한 카메라·삼진 모음이라 선택 편향이 있다.
- 2026-09-23 **D49** 사용자 지시에 따라 첫 화면의 우선순위를 확정한다: 투구 전에는 가장 유리한 구종·목표 위치, 투구 후에는 사전 추천과 실제 비교, 중요 사건 후에는 기여 비중·근거를 제공한다. 투구 전 후보별 비율/수치 비교와 추천 이유 표시는 후순위다. 내부 후보 평가·보정·연구 검증은 유지한다. CV 의도 모듈은 팀원 전담(D46 재확인)이며, 우리는 기존 `IntentEstimate` 계약(D46~D48)을 소비하는 서비스·추천·평가·사건 분석·UI를 병렬 개발한다. 계약을 만족하는 모듈이 제공된다는 개발 가정과 실제 품질 검증 완료를 구분한다. 개발용 샘플로 연결·계산을 진행하고, 제구 학습·실사건 기여도 검증은 실제 산출물 인수 뒤 통과시킨다. M1 전체 완료를 모든 개발의 선행조건으로 두지 않는다. 일정 대신 의존성 관리(D44)는 유지하며 업무별 선행조건·합류 기준은 `docs/plan.md`에 둔다. 이번 변경은 업무 계획이며 모델 재학습·UI 변경·CV 연결 완료를 뜻하지 않는다.
- 2026-09-23 **D50** 사용자 제안에 따라 학습은 작은 데이터셋에서 시작해 통과 기준에 따라 점진 확장한다. S0 실행 검증 → S1 소규모 실제 학습 → S2 선수·기간·상황 확장 → S3 전체 지원 범위 검증. 초기 성공은 학습·추천·평가 경로의 정확성과 재현성이고, 최종 성공은 공통 정책 평가의 성능 개선이다. 작은 표본에서 유의한 우위가 나올 때까지 표본을 바꾸지 않는다. 결과를 보기 전에 선수/경기 선정·시간 분할·확장 규칙을 고정하고 최종 평가셋은 별도 보존한다. 데이터 규모 효과와 모델/피처 효과를 분리하며, 개발용 CV 샘플은 연결 검사에만 쓴다. 구체적인 규모는 가용 자료·희귀 사건 수·실행 비용을 확인해 config에 기록한다. 이번 결정은 계획 반영이며 학습 실행은 아니다. docs/plan.md의 단계별 통과 기준을 따른다.
- 2026-09-23 **D51** A 공통 준비 C0 v1에서 서비스 가치 단위를 `defense-we-pa-v1`로 확정: 초기 수비 팀의 최종 승리 확률 [0,1], 현재 타석 종료까지 계획 + 동결 WE continuation, 차이는 100×ΔW %p. 이닝 종료 교체 평가와 RE24 동결 연구는 별도 범위/표이며 합산하지 않는다. 기존 IntentEstimate v1·투구 키·좌표·불변 사전 추천을 재사용한다. 현재 `observer-zone-v1`은 관측 delivery 재가중 근사이고 목표 개입 효과는 미식별이다. 작은 실제 자료의 선정·분할·평가 규칙을 결과 계산 전에 고정하며 2026을 추가 열람하지 않는다. 계약 `docs/contracts/C0-v1.md`, 평가 사양 `docs/contracts/S0-S1-v1.md`, 시작 상태·결과는 `docs/handoffs/A.md`.

- 2026-09-24 **D52** 사용자 우선순위를 현재 고정 범위 ML 예측 성능 개선 → 지원 투수 확대·재검증 → 실제 CV/의도 통합 → 팬 이해 평가·UI/UX → 실경기 운영으로 변경한다. 이번 세션은 계획으로 끝내고 다음 대화에서 실험한다. 논문 원래 과제/지표의 재현과 같은 정보·자료의 공통 예측 비교를 분리하고 데이터·backbone·학습 방식 변경/실패를 config·seed·버전·비용과 함께 기록한다. 예측 모델 연구 후보는 NLL/Brier 등으로 선정할 수 있으나 서비스 정책 우위/채택은 별도 평가를 요구한다. 2026·최종셋 개발 금지와 원자료·동결 모형·runtime_src·사용자 기록 보존은 유지한다. 판단/독립 검토는 Astra, 구현·실험 실행은 Sol medium, 다음 연구의 재귀 위임을 허용하되 전체 동시 한도·무거운 실행 1개를 지킨다. 상세 계획: docs/handoffs/ML-experiments-next-session.md.

- 2026-09-24 **D53** 사용자가 실제 실행 전 전체 실험 설계를 요청하고 첫 검증 범위를 **MLB 정규시즌 선발·불펜 전체**로 선택했다. 연구 목표는 상황별 구종·목표 위치 확률 정책이며 데이터량, 선수 유형별 정보 공유, 입력 특성, LSTM/Transformer 등 구조, 강화학습과 논문 기반 추가 방법을 비교한다. 기존 수비 WE 목표와 ML0 재현→ML1 첫 데이터 비교는 유지한다. 구체 설계는 `docs/ML_EXPERIMENT_DESIGN.md`: 결과 예측·행동 모방·정책 가치를 분리하고, 기존 6명은 출발점으로 보존하며 MLB 전체 검증을 별도로 둔다. 모델 후보·실험 순서·수치 문턱은 설계 제안이며 사용자가 모든 세부안을 승인했다는 뜻이 아니다. 이번 변경은 문서만이며 새 학습·평가·정책 채택은 미실행이다. D52의 자료/동결 자원 보존 제약은 유지한다.
