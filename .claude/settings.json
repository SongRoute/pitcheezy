{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.py|*.yaml|*.yml) cd \"$CLAUDE_PROJECT_DIR\" && .venv/bin/pytest tests -q -x 2>&1 | tail -2 ;; esac; true",
            "timeout": 120,
            "statusMessage": "pytest tests -q"
          }
        ]
      }
    ]
  }
}
