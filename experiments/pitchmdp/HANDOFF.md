# 다음 세션 시작점

## 현재 논의: 오늘의 목표와 정책 비교 로드맵

- 최신 요청은 결과 확인과 다음 실험 계획 수립이다. **[TODAY_ROADMAP.md](TODAY_ROADMAP.md)**에 목표·증거·진행 조건을 정리했다. 이 계획 수립 중 추가 실험을 시작하지 않았다.
- 오늘은 타자 성향·필수 경기 상황을 반영한 타석 추천 연구의 방향을 판단한다. 군집/윈도우 최적값이나 특정 신경망 채택 자체는 성공 기준이 아니다.
- 다음 핵심 제안은 가치/종결 전이의 검증을 먼저 준비한 후, 같은 예측기의 **RE/WE 목적×한 구 개입/타석 계획**2×2 정책 비교다. 독립된 경기로 학습한 평가 모델에서도 차이가 유지되는지 확인해야 한다.
- 추천 안정성은 제외 범위로 유지한다. 우선 구종만 선택하고 물리 분포를 평균내는 제한 정책을 제안했으며, 구종+목표 위치라는 최종 목표의 달성으로 간주하지 않는다.
- 위 계획이 아래 과거 항목의 다음 우선순위(ID 추가 등)보다 최신이다. MLP의0/5구 비교를 Transformer 이력 효과의 부재로 일반화하지 않는다.

## 완료: 과거 투구0/5구 × 타자 표현 검증 (2026-09-21)

- 사용자가 작성된 실행기를 직접 실행하라고 승인했다. M4 MPS에서 신규60개 모델을 약32분16초에 순차 학습하고 기존5구60개를 재사용했다. 모든 프로세스 종료.
- **[HISTORY_BATTER_VALIDATION_RESULTS.md](HISTORY_BATTER_VALIDATION_RESULTS.md)**에 해석과 핵심 표, SSD의 `runs/history-batter-validation-v1/RESULTS.md`에 전 결과·하위집단을 보존했다. 기계 결과 `results.json`, 로그 `run.log`.
- 타자 표현6종(연속 성향, 군집K3/5/10/20, 성향+K5)×동일 타석의 이전0/5구를 비교했다. 주자·아웃 등 필수 상황과 타자 성향을 유지하고 과거 투구 물리 토큰/마스크만 제거했다. 추천 안정성은 이번 범위가 아니다.
- **0구−5구 비교6개 모두 2024·2025 연도별 보정95%구간에0 포함.** 5구 추가 이득 미확인, 동등성 증명 아님. 실제 이전5구 이상 표본은 8.7% /9.1%.
- 성향+군집5와 연속 성향 단독의 차이도 0/5구 두 연도 모두 보정 구간에0 포함. 2025 5구에서는 군집 단독K3·K5가 연속 성향보다 나쁜 방향의 구간을 보였지만 2024 미재현. 최적 K를 선언하지 않는다.
- 기존5구 점수 재현, 모든 단계/원소스/아티팩트 해시, 저장된 투구별 예측에서 점수·경기bootstrap구간 재계산 통과. 전체157개 테스트 통과. 모델 가중치로 독립 전량 재추론한 감사와는 구분한다.
- 이미 본 두 연도의 탐색 결과이고 TRAIN 중첩, 평가용 적격 필터 유지. 실제 추천·승률 개선의 검증은 아니다.
- 실행기 `scripts/run_history_batter_validation.py`와 설계 `docs/HISTORY_BATTER_VALIDATION.md`는 완료 run의 소스 해시로 동결했다. 수정하지 않는다. 동일 명령 재실행은 완료 아티팩트 해시만 확인한다.
- 현재 다음 판단: 연속 성향 단독을 단순한 후보로 두되 미관측 시기에서 선택·확인할 것. 과거 투구 이력을 더 길게 늘리기 전에 현재0/5구의 효과가 불확실한 이유(지원 표본·상황별 이질성)를 분리해 검토할 것.

## 완료: 타자 표현·이력 길이 비교 (2026-09-21)

- 사용자 수정에 따라 주자·아웃과 타자 정보를 유지하고 표현 방식과 이력 길이를 각각 비교했다.
- 결과: **[REPRESENTATION_HISTORY_RESULTS.md](REPRESENTATION_HISTORY_RESULTS.md)**. 신규110개 MLP(11설정×2연도×5seed), 약52분에 완료. 진행 중인 학습/추론 없음.
- 타자 축: 이전5구를 고정해 좌·우타만 / 개인 ID / 성향6개+신뢰도6개 / soft군집K3·5·10·20. 기준은 기존 성향12+K5.
- 이력 축: 기존 타자 표현을 고정해 같은 타석의 이전1·2·3·4·5구. 6개 슬롯과 모델 크기를 유지하고 오래된 값·마스크 제거. 이전 타석을 넘는 투수 이력은 미실험.
- 기존 전체 입력 혼합의 로그손실2024 1.505276 /2025 1.491226. 좌·우타만1.510125 /1.496533, 개인 ID1.508858 /1.493976. 두 비교 모두 각 연도 타자7개 비교군 보정95%구간에서 기존 입력이 우세했다.
- 연속 성향 단독·군집 단독과 기존 구성의 차이는 모두 보정 구간에0을 포함했다. 군집 단독에서는K10의 평균이 두 연도 모두 가장 낮지만 최적 K라는 검증은 아니다.
- 이전1~4구와5구의 차이는 두 연도 모두 보정 구간에0을 포함했다. 5구의 추가 이득은 미확인이고 동등성 증명도 아니다. 이전5구 이상 표본은 약8.7% /9.1%에 그쳤다.
- ID와 성향 방식은 용량뿐 아니라 과거 증거량·평가 중 성향 갱신 여부가 달라, 순수한 인코딩 효과로 해석하지 않는다. 이미 관측한 역사 데이터의 탐색 결과다.
- 전체148개 테스트 및110모델 독립 감사 통과. `final_audit.json`에 표본·보정·혼합·구간·해시 검증 저장. 원 매니페스트는 보존하고 보고서/감사/최종 소스는 별도 completion manifest로 보존한다.
- 실행: `/Volumes/T7 Shield/pitcheezy/pitchmdp/runs/representation-history-20260921T054642Z`.
  로그: 같은SSD `logs/representation-history-20260921T054642Z.log`.
- 새 코드 `scripts/run_representation_history.py`, `scripts/representation_adapters.py`; 사전 조건 `docs/REPRESENTATION_HISTORY_PROTOCOL.md`는 해시 동결. 감사/보고서는 `scripts/audit_representation_history.py`, `scripts/report_representation_history.py`.
- 다음 우선순위: 성향 고정 상태에서 ID 추가 → 성향 갱신 시점을 고정한 대조 → 타석 경계를 넘는 투수 이력. 아직 미실행. DEV로 K·윈도우의 승자를 고르거나 두 축의 상호작용을 검증했다고 주장하지 않는다.

## 완료: 날짜 재검증 + 공정한 혼합 비교 (2026-09-21)

- 사용자가 1·2번 결합 실험을 승인했다. 새 실행기 `scripts/run_temporal_blend.py`, 사전 조건 `docs/TEMPORAL_BLEND_PROTOCOL.md`.
- 2024/2025 각각 TRAIN 이닝만으로 선발6명 선정, 평가 이닝 조건 없음. 두 모델을 새로 학습한다.
- Transformer128·MLP128 각각 seed42–46, 공통400개 물리 표본, 같은 상황 빈도 기준선과 각각 혼합.
- CAL을 May1–15 조기 종료 / May16–31 온도 / June 혼합으로 분리. 평가 Jul–Sep.
- 2024 평가7737구, 2025 평가5048구. 아래 이전 결과와 코호트·CAL 조건이 달라 직접 수치 비교하지 않는다.
- 결과: **[TEMPORAL_BLEND_RESULTS.md](TEMPORAL_BLEND_RESULTS.md)**. 실제 실행34분13초, 두 연도·20모델 완료.
- 로그손실:2024 기준선1.517402 / MLP혼합1.505276 / T혼합1.504443;2025 기준선1.505737 / MLP혼합1.491226 / T혼합1.490642.
- 두 혼합 모두 기준선 대비 LL·Brier 개선의95%경기구간이0 아래. T−MLP 혼합은2024−0.000833[−0.001915,+0.000196],2025−0.000584[−0.002555,+0.001297]로 우열 미확정.
- 평균fit 시간 MLP22.7초, T158.3초. 후속은 사용자 수정에 따라 필수 상황을 유지한 타자 표현·이력 길이 비교로 완료했다(위 최신 결과). Transformer 교차 확인은 아직 미실행이다.
- 전체138개 테스트 및 독립 데이터·예측·혼합·bootstrap·아티팩트 해시 감사 통과. `final_audit.json`에 결과 저장. 진행 중인 학습/추론 없음.
- 이전 코드·결과는 보존한다.
- 실행 경로: `/Volumes/T7 Shield/pitcheezy/pitchmdp/runs/temporal-blend-20260921T050400Z`.
  로그: 같은SSD `logs/temporal-blend-20260921T050400Z.log`. 실제 시작 시간은 run의 `config.json`을 따른다.
- 신규 단위 테스트10개 통과. 독립 검증기 `scripts/audit_temporal_blend.py`는 모델 추론 없이 표본·예측·혼합·bootstrap을 재계산한다.
  재개는 이 temporal run 경로에 한정한다. 기존 다른 종류의 run 경로를 지정하지 않는다.

## 최신: 후속 실험 배치 완료 (2026-09-21)

- 사용자가 완료 후에도 권장 우선순위대로 개선 실험을 바로 이어서 진행하도록 승인했다.
  별도 확인 질문 없이 이 승인 범위에서 실행한다.
- 진행 기록: **[FOLLOWUP_RESULTS.md](FOLLOWUP_RESULTS.md)**.
- `runs/sequence-20260921T020823Z/robustness-20260921T023616Z`에서 seed42–46 고정 반복.
  표본·정규화·타자 유형·공동 물리 표본은 seed42 고정이며 신경망 seed만 바꾼다.
- `replication`20모델, `capacity`5모델, `clusters`5모델 모두 완료. 독립 감사 통과.
  군집 유사도5개의 추가 이득은 확인되지 않았다. 경기 상황·과거 타자 성향 묶음의 효과는 남았다.
  main package, 원 실행기, 후속 실행기, `docs/NEXT_EXPERIMENTS_PROTOCOL.md`는 해시 동결돼 있다.
  완료 체크포인트는 파일/설정/표본 해시 확인 후 재사용한다.
- 카운트 혼합 완료: 후속run `calibration-20260921T031450Z`, 주LL혼합1.509390.
  구종 빈도 기준선 완료: 주run `frequency-baselines-20260921T030038Z`.
  구종 혼합과 추가 주자·아웃 빈도 혼합도 완료. 후자는 로그손실1.502661/Brier0.727579.
  저장 경로는 `calibration-20260921T031450Z/frequency-blend-20260921T031904Z/context-frequency-20260921T033016Z`.
- 당일 구위 적응 완료: 주run `delivery-adaptation-20260921T032817Z`.
  CAL이 정적 분포를 선택했으므로 적응은 채택하지 않는다. 사전100표본 진단에서
  동일T42의 로그손실이1.518155→1.506911로 달라져 적분 정밀도를 우선 재검증한다.
- 100표본30개 및 T42의400표본 진단 완료: 후속run `integration100-20260921T033810Z`.
  약216.6초, 학습 가중치 동일성 포함 독립 감사 통과. T−작은MLP의구간은0을 포함한다.
  T42 로그손실25/100/400 =1.518155/1.506911/1.503577로 수치 민감도가 남았다.
  새100표본 주자·아웃 혼합 완료: `integration100-20260921T033810Z/calibration100/context-reblend-20260921T034254Z`.
  CAL 주비중54.24%, 로그손실1.498928/Brier0.726035. 원25표본 결과는 보존한다.
- **마지막 진단 완료:** T42의 DEV만 원 학습 풀의 모든 물리 벡터로 정확히
  평균냈다. 온도는 사전에 완료한400표본 CAL값 그대로 고정하고,
  같은 온도로25/100/400/전체 평균을 비교한다. 새로운 보정·모델 선택·신경망 학습 없음.
  주run `exact-pool-profile-20260921T034508Z`에서 전체 DEV1,330,692쌍(전역fallback0)을 확인했다.
  CAL까지 모두 정확히 적분하는30모델 재실험은 비용 때문에 이 배치에서 진행하지 않는다.
  결과는 주run `exact-dev-diagnostic-20260921T035425Z`.
  같은 온도에서25/100/400/전체 로그손실1.518215/1.506912/1.503577/1.503131.
  전체 풀의 모든 후보·확률·해시 독립 감사 통과.400표본도 투구별 확률 차이는 남는다.
- 전체128개 테스트 통과(`logs/tests_followup_final.log`). 진행 중인 학습·추론 프로세스 없음.
  첫 실제 결과는 착수11분, 이번 후속 배치는 약2시간35분 안에 완료했다.
  새 작업은 `FOLLOWUP_RESULTS.md` 맨 위 현재 결론과 마지막 다음 라운드 안내를 우선한다.
- 실제 이력 추천12개와 같은 시퀀스로 타석 종료까지의 모의 평가 완료:
  `sequence-rollout-20260921T032938Z`, 독립 난수/값/해시 감사 통과, 검열0.
  모델 내부 선택의 난수 오차만 평가한 결과이며 실제 승률 개선이 아니다.
- 제구/물리량/깊이 민감도72조건 완료. 모든 사례에서 어느 조건에서는1위가 달라졌다.
  실제 타석 중간 사례12개는 추천 점수 전에 고정해 `midpa_selection.json`과 `midpa_cases.pkl`에 저장했다.
  이를 만든 `run_sequence_midpa.py`도 재생 동일성 해시가 있으므로 임의로 수정하지 않는다.
- 추가 CPU 진단: 같은 병살 합법성 규칙을 적용하면 경기 상황 이득이 약 절반으로
  줄지만5초기값에서도 양수다. 구종 기준선 대비 혼합 이득의56%가 병살에서 왔고,
  기준선에도 주자·아웃을 제공하면 병살 기여율은19.6%로 줄어든다. 기존 예측은 보존한다.

## 이전: 첫 시퀀스 실험 완료

- **[SEQUENCE_RESULT.md](SEQUENCE_RESULT.md)**는 첫 시퀀스 결과다. 후속은 위 문서를 우선한다.
- 사용자가 Takamido & Nakamoto 기반 시퀀스 구조 → 모든 투구 결과 → 타석 WE 확장을 승인했다.
- `runs/sequence-20260921T020823Z`: 11:12:56 KST 주 예측 실험 완료.
  같은 입력의 MLP 1.523920 대비 Transformer 로그손실 1.518155,
  차이 −0.005765, 경기 bootstrap 95% [−0.008967, −0.002542].
  Brier는 카운트 기준선이 더 좋고, 단일 seed·이미 본 DEV의 탐색 결과다.
- 기존 MLB 6명을 고정하고, 타자 ID 대신 과거 성향과 5개 유형 유사도를 사용했다.
- 5구 이력+후보 공, 실제 현재 공의 물리량을 넣지 않는 적분 평가,
  2구 분기 후 카운트 continuation의 근사 WE 추천까지 구현·실행했다.
  완전한 시퀀스 타석 최적화나 실제 승률 개선으로 해석하지 않는다.
- 63개 테스트 및 저장 데이터·소스·예측·bootstrap 감사 통과.
- 추가 탐색 제거 실험도 완료: 같은 Transformer에서 경기 상황을 빼면 로그손실
  1.527314, 타자 성향·유형 묶음을 빼면 1.523423으로 전체 입력 1.518155보다 나빴다.
  두 차이의 경기 bootstrap 구간도 같은 방향. 주 run 아래
  `ablations-20260921T022219Z`에 별도 저장했고 독립 감사 통과.
- 다음 연구 단계는 복수 seed·평가 기간 확인, 개별 요소/군집의 기여 분리,
  제구 가정 민감도, 더 긴 시퀀스 계획과 독립 정책 평가다.
  이미 관측한 DEV에서 확인한 값을 새 확증 결과로 부르지 않는다.
- 새 위임은 사용자 요청대로 GPT-6 Astra `high`; 동시 신경망 학습은 1개로 유지한다.

## 2026-09-21 초기 파일럿 기록

- **착수 10:23:42 KST, 첫 실제 결과 완료 10:34:37 KST (약 11분).**
- 당시 진입점: **[REVISED_PILOT.md](REVISED_PILOT.md)**와 **[README.md](README.md)**.
- 사용자 최신 방향을 반영한 MLB 6명·타자 연성 유형 모델도 10:44:49 KST에 완료했다.
  산출물은 `runs/first-20260921T014425Z`이다. 타자 ID는 과거 연결에만 사용한다.
  관측 예측은 단순 기준선보다 나빴고, 수정 보고서에 실패·제거 실험도 기록했다.
- 연구 초점은 기존 논문에서 빠진 요소의 추가 효과다. 원문 확인상 단순 B/C/D 입력 자체는
  선행 사례가 있으므로 `docs/RESEARCH_GAP_REVIEW.md`의 예측 효과·가치 효과 구분을 따른다.
- 새 위임 작업은 사용자 요청에 따라 GPT-6 Astra `high`로 낮췄다.
- 데이터·모델·추천 산출물: `/Volumes/T7 Shield/pitcheezy/pitchmdp/runs/first-20260921T013426Z`.
- 실제 LAD 선발 아웃 집계로 밀러·글래스노우·스톤·야마모토를 고정했다.
  평가 기간에는 글래스노우·야마모토만 선발 표본이 있어 나머지 둘은 미평가로 남겼다.
- 결과 예측→실제 경기 승패 WE→타석 계획→구종/목표 추천→날짜 분리 평가를 연결했다.
- 첫 투구 전 예측은 단순 기준선을 개선하지 못했다. 모델 내부 정책 이득을 실제 효과로 해석하지 않는다.
- 아래는 초기 세팅 당시의 기록이다. 미구현 여부는 최신 README와 FIRST_RESULT를 우선한다.

---

사용자는 이번 세션에서 초기 세팅을 하고 다음 세션에서 개발·실험을 시작하기로 했다.
**6시간 이내(가능하면 더 빨리) 첫 결과를 검토하고, 24시간 이내 초기 방향을 정한다.**
시간은 다음 세션의 본격 착수 시각부터 계산한다. 현재 학습 프로세스는 없다.

## 먼저 읽을 것

1. RESEARCH_CHARTER.md: 장기 목표, 승률 목적, 데이터·평가 의미.
2. DAY1_PLAN.md: 이번 24시간 범위와 6시간 첫 결과.
3. configs/local.json: 로컬 경로, 선수 선정 기간의 확정 상태.

사용자의 최신 요청이 첨부 CLAUDE/DESIGN/TASKS 및 이전 계획보다 우선한다.
기존 pitcheezy의 라벨·캐시·RE24 모델을 새 설계와 호환된다고 가정하지 않는다.

## 초기 세팅

- 코드: 이 하위 프로젝트. 원본 pitcheezy 구현과 분리.
- Python: 저장소의 기존 `.venv/bin/python` 재사용. 패키지 설치·업그레이드 없음.
- 대용량 출력: `/Volumes/T7 Shield/pitcheezy/pitchmdp`.
- `scripts/preflight.py --prepare`: SSD 마운트·쓰기, 허용 원본의 parquet 메타데이터,
  Torch 장치 실행, 디스크 여유, 설치 버전을 점검하고 SSD reports에 기록.
- 설치 버전 목록은 현황 스냅샷이며 완전한 의존성 lockfile이 아니다.
- 기존 `runs`와 원본 경로는 Google Drive 연결이다. 새 출력을 기존 runs에 쓰지 않는다.
- 원본은 아직 SSD로 복사하지 않았다. 필요시 허용된 세 파일만 복사하고 해시를 검증한다.
- 2026 원본은 열지 않는다. 원본 행 프로파일·누수 검사·선수 집계는 다음 세션 작업이다.
- 실행 큐, 학습기, 체크포인트 재개, W&B 연결은 아직 미구현이다.
  JSON 설정만으로 기존 프로그램의 출력·로깅 경로가 자동 변경되지는 않는다.

2026-09-21 초기 점검 통과: T7 실제 쓰기/읽기, 2023–2025 parquet 메타데이터,
기존 Python 환경, MPS 2×2 행렬 연산, tmux/uv 경로.
환경 전체 학습 호환성 또는 모델 검증이 끝났다는 의미는 아니다.
점검 기록: `/Volumes/T7 Shield/pitcheezy/pitchmdp/reports/setup-20260921T012212640861Z.json`.
이 파일에 패키지 버전 목록도 포함한다. 코드·설정 외 원본 복사와 학습 실행은 하지 않았다.

## 점검 명령

저장소 루트에서:

```sh
.venv/bin/python experiments/pitchmdp/scripts/preflight.py
```

SSD 재연결 후에도 이 명령을 먼저 실행한다. SSD 미마운트 시 내장 경로로 대체하지 않는다.

## 다음 세션의 첫 작업

1. 선수 선정 기간은 사용자 확정: **2023-05-15~2025-04-30**. 해당 기간 LAD 선발 등판 이닝 상위 4명을 집계한다. 재확인 질문은 필요 없다.
2. 착수 시각을 기록한다. 허용 데이터의 해시·분할·선발 이닝 집계 근거를 고정한다.
3. 결과 예측→타석 계획→추천→평가를 작은 표본에서 먼저 연결한다.
4. 6시간을 기다리지 말고 실행 가능한 첫 결과가 나오면 사용자에게 제시한다.
5. 이후 Full/−B/−C, WE/RE, myopic/타석 계획을 시간 예산 안에서 비교한다.

첫 결과의 최소 기준은 DAY1_PLAN.md를 따른다. 순위만 출력하거나 장난감 데이터만
통과한 것을 실제 데이터 기반 첫 결과로 보고하지 않는다. 모델 의존 전략 가치와
관측 데이터에서 확인한 예측 성능을 분리한다.

## 초기 착수 요청 (이하 과거 기록)

> experiments/pitchmdp/HANDOFF.md와 DAY1_PLAN.md를 읽고 시작해줘.
> 이번 착수부터 6시간 이내, 가능하면 더 빨리 실제 데이터 기반 첫 추천·평가 결과를 보여줘.
> 다저스 선발 이닝 상위 4명, 타자 제한 없음이며 승리 확률 목적의 타석 단위 연구 MVP야.
> 초기 세팅 점검 후 작은 end-to-end 실행부터 연결하고, 큰 학습 전에 데이터·솔버를 검증해줘.
