{
  "protocol": "ml_architecture_v1",
  "experiment_id": "EXP-P3-001",
  "parent_run": "/Volumes/T7 Shield/pitcheezy/pitchmdp/runs/ML-MATRIX-20260924/EXP-P2-001-v2",
  "parent_preparation_sha256": "131c2e887895d863763a1514f9cceb1caa1b82c844dbdcb8148e1951619dc1fb",
  "seeds": [0, 1, 2],
  "data_sample": "d100",
  "history_length": 5,
  "draws": 400,
  "device": "auto",
  "width": 128,
  "neural": {"epochs": 30, "patience": 5, "batch_size": 1024, "learning_rate": 0.0005},
  "lightgbm": {"epochs": 300, "patience": 30, "learning_rate": 0.05,
    "params": {"num_leaves": 31, "min_data_in_leaf": 100, "lambda_l2": 1.0}},
  "registration": {
    "matrix_rows": ["MX-A0", "MX-A1", "MX-A2", "MX-A3", "MX-A4", "MX-A5", "MX-A6", "MX-L5"],
    "output": "/Volumes/T7 Shield/pitcheezy/pitchmdp/runs/ML-MATRIX-20260924/EXP-P3-001",
    "control": "A0-MLP",
    "primary_candidates": ["A1-linear", "A2-lightgbm", "A3-lstm", "A4-gru", "A5-melville", "A6-transformer"],
    "hypothesis": "Architecture changes improve held-out pre-pitch outcome probabilities under identical enriched H5 information and D100 samples.",
    "features": "Same ordered H5 physical, past pitch-type, past outcome, masks, current candidate type, and common player/game context for every family; current outcome token zero. Current physical quantities integrated over fixed TRAIN delivery draws.",
    "bridge": "A0 differs from D1-100 in enriched tokens and implementation; cross-stage difference is descriptive, not an architecture-only effect.",
    "calibration": "Same parent temperature and blend CAL; existing float32 JointDelivery temperature objective preserved; final softmax/integration float64; ensemble then June frequency blend.",
    "primary_predictor": "Arithmetic mean of three independently temperature-calibrated member probabilities, then June-fitted frequency blend.",
    "seed_direction_predictor": "Each seed has its own June-fitted frequency blend.",
    "bootstrap": {"unit": "whole game", "estimand": "pitch-weighted paired mean loss", "draws": 10000, "seed": 20260924},
    "multiplicity": "Six candidate-minus-A0 one-sided null-centered bootstrap tests, Holm alpha .05.",
    "minimum_nll_improvement": 0.003,
    "maximum_brier_difference_ci_upper": 0.001,
    "required_improving_seeds": 2,
    "cost": "Sum of load+fit+temperature seconds for all three members on this host; raw costs and tradeoffs descriptive; no formal C claim without registered noninferiority multiplicity analysis.",
    "secondary_comparison": "GRU minus LSTM, descriptive only.",
    "followup_ranking": "At most two N-passing candidates, ascending primary DEV NLL, ties resolved by lower total fit+temperature time. No HPO in this screen; followup needs new registered family.",
    "max_full_fits": 21,
    "hpo_trials": 0,
    "max_heavy_jobs": 1,
    "single_member_wall_limit_seconds": 7200,
    "batch_wall_budget_seconds": 28800,
    "resource_gate": "After parent family completion: same-data tiny profile of each architecture before full fits; do not silently reduce D100, seeds, or draws.",
    "lightgbm": "4.6.0 macOS arm64 isolated --target dependency; DYLD_LIBRARY_PATH points to torch/lib for one OpenMP runtime; KMP_DUPLICATE_LIB_OK forbidden.",
    "scope": "Previously exposed 2025 C6 retrospective eligible DEV; no whole-MLB robustness, original-paper replication, final-set, or policy-effect claim. Missing class/group evidence stays null.",
    "raw_provenance": "Processed/cache bytes verified. Supplementary raw2023/2024 hashes verified; raw2025 cloud read timed out. No ingestion reproduction claim."
  }
}
